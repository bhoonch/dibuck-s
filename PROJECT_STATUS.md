# 디벅 (dibuck) — 프로젝트 진행 상황

아파트 관리사무소 직원용 모듈 구독형 SaaS.
(최종 업데이트: 2026-07-27 — 관리자 콘솔 고도화 + 공지 타겟팅·무료 체험 완료, 5단계 로드맵 재편. 마지막 커밋은 `7f7bec0`이고 **이후 작업은 아직 커밋되지 않음**)

## 완료된 단계 (1~4단계)

### 1단계 — 디자인 시스템
- 2계층 컬러 토큰: primitive(Slate 팔레트 + #2563EB primary) → semantic(primary, success, warning, danger, info, surface 등), `src/app/globals.css`
- 디자인 원본: 클로드디자인 프로젝트 **"건물관리자 통합 대시보드"** 의 `관리사무소 대시보드.dc.html`
- 폰트: Pretendard 스택 + JetBrains Mono(숫자·날짜·문서번호), 루트 17px(쉽고 큰 UI)
- 공용 컴포넌트: shadcn/ui 14종 + DataTable(정렬·페이징), PageHeader, EmptyState, ConfirmDialog, DatePicker, FileUpload, SideNav, AppHeader

### 2단계 — 코어
- 멀티테넌트(모든 테이블 tenant_id) + 이메일 로그인(jose JWT 쿠키) + RBAC
- 모듈 레지스트리(DB): 구독 상태에 따라 사이드바 노출/잠금. 새 모듈 = 레지스트리 등록 + `src/lib/module-icons.ts` 아이콘 한 줄
- 공통 문서함: documents 단일 테이블, 한글 문서번호 자동 채번(`공지-2026-0001`), 통합 검색
- 알림 센터: 사용자별 fan-out, 헤더 미읽음 배지
- 세대 마스터(Unit): 설정 > 세대 관리에서 엑셀 업로드(샘플 파일 제공) — 모든 모듈이 재사용

### 3단계 — 사용자 대시보드
- 홈: 오늘의 할 일 위젯 4종(결재 대기·계약 만료 D-30·미처리 민원·점검 예정), 할 일 리스트, 처리 현황, 최근 문서, 모듈 바로가기
- 모듈 런처(`/modules`): 미구독은 잠금 + 구독 유도
- 설정 5탭: 단지 정보 / 세대 관리 / 직원 관리 / 결재선(가로 플로우 시각화) / 구독 관리(카드 그리드, 구독/해지 토글)

### 4단계 — 관리자 콘솔 (`/admin`)
- 지표: 가입 단지, MRR, 모듈별 구독 수(막대), 최근 가입/해지
- 단지 관리: 목록/상세, 이용 중지(로그인 차단), 모듈 활성화 토글(feature flag)
- 모듈 관리: 레지스트리 CRUD
- 공지 관리: 전 단지 배너(한 번에 하나 활성)
- 임퍼서네이션: 세션 JWT에 `impersonating` 플래그 + 관리자 userId 유지. 상단 배너 없이 **양쪽 사이드바 하단 같은 자리에서 왕복** — 관리자 콘솔 "사용자 대시보드" → 사용자 화면 "관리자 대시보드". 개요 페이지의 진입 버튼·테스트 안내 박스는 중복이라 제거함(단지 상세의 "사용자 화면으로 전환"은 유지)

## 합의된 규칙 (새 화면 작성 시 준수)

| 항목 | 규칙 |
|---|---|
| 권한 vs 직책 | 권한(Role)은 소장/경리/직원 3종 유지, 직책(User.title)은 자유 텍스트. 직책을 enum에 추가하지 말 것 |
| 버튼 3단계 | `lg`=폼 주요 액션(저장·업로드, h-10 min-w-32) / `default`=카드 액션(구독·해지) / `sm`=테이블 행 안 |
| 타이포 (전 화면) | **공용 체계만 사용** — `text-2xl`(페이지 제목) / `text-lg`(섹션 제목) / `text-sm`(본문·표) / `text-xs`(메타·라벨) / `font-mono text-3xl`(KPI 숫자). `text-[13px]` 같은 px 하드코딩 금지 — **관리자 콘솔도 예외 없음**. 디자인 시안에서 가져오는 건 레이아웃·구성뿐 |
| 레이아웃 폭 | 테이블·목록은 전체 폭, **입력 폼은 `max-w-2xl`(≈714px)** — 라벨과 입력칸이 한눈에 묶여 보이는 한계 폭. 더 넓히면 라벨↔값 시선 이동이 길어지고 짧은 입력칸이 화면을 가로질러 늘어난다. 폭이 더 필요한 필드는 폼을 넓히지 말고 **카드 안에서 2열 그리드**로 풀 것 |
| 금액 입력 | 천단위 콤마를 보여주는 `PriceInput`(client) 사용. 서버는 `parseWon()`으로 숫자만 걷어낸다 — `Number("39,000")`은 NaN이라 안 걷으면 가격이 조용히 0원이 된다 |
| 간격 체계 | 카드 헤더 12×16px + gray-100 구분선, 본문 16px, 테이블 행 12×16px |
| 홈/헤더 | 모듈 종속 버튼 금지 — 모듈 진입은 할 일 리스트·모듈 바로가기가 담당 |
| 문서 컨벤션 | 모듈은 `createDocument()`(자동 채번+알림) 사용, 할 일 위젯은 type+status+dueDate로 집계 |
| 입대의 결재 | 5단계 전자결재 때 "입주자대표" 권한 추가로 기존 결재선에 통합 (별도 시스템 금지) |

## 관리자 콘솔 고도화 — 완료 (2026-07-27)

디자인 원본: 클로드디자인 **"Copy of 건물관리자 통합 대시보드"** 의 `관리사무소 대시보드.dc.html`
(관리자 콘솔 화면은 이 파일에만 있음 — v2·v3·v4는 사용자 홈 시안이라 관리자 영역 없음)

### 메뉴 6개 (디자인 `adminTabs` 그대로)

| 메뉴 | 경로 | 내용 |
|---|---|---|
| 개요 | `/admin` | 사용자 대시보드 테스트 카드, KPI 4(가입 단지·MRR·모듈 구독·이번 달 해지), 단지 관리 표, 모듈별 구독 바 |
| 가입자 현황 | `/admin/tenants` | KPI 4, 단지 목록(월 요금 포함) + 단지 등록, 최근 가입·해지 타임라인, 모듈 구독 전환율 |
| 수입 현황 | `/admin/revenue` | KPI 4(MRR·ARR·단지당 평균·활성 구독), 월별 MRR 추이 7개월 막대, 모듈별 수입 |
| 모듈 관리 · 가격 | `/admin/modules` | 인라인 가격 편집 + 행별 "가격 저장", 모듈명 클릭 시 전체 수정 |
| 공지 관리 | `/admin/announcements` | 대상·게시 기간·상태(예약/게시중/종료) 표, 새 공지 작성 |
| 고객 문의 | `/admin/support` | 단지·유형·상태 표, 답변 완료 토글. 사이드바에 답변 대기 배지 |

### 적용 규칙

- **타이포·버튼은 사용자 대시보드와 동일한 공용 체계**(rem, 루트 17px). 관리자 화면도 `text-2xl`(페이지 제목) / `text-lg`(섹션 제목) / `text-sm`(본문·표) / `text-xs`(메타·라벨) / `font-mono text-3xl`(KPI 숫자)를 쓰고, 버튼은 공용 `buttonVariants`의 3단계(lg=폼 주요 액션 / default=페이지·카드 액션 / sm=표 행)를 그대로 위임한다. 디자인에서 가져온 것은 **레이아웃·구성**(메뉴 6개, 2fr:1fr 그리드, 표 컬럼, 섹션 구성)뿐
- 공용 프리미티브는 `src/app/admin/ui.tsx`: `PageTitle` `Section` `Kpi` `Pill` `Bar` `TwoLine` + `tableHead`/`tableRow`(그리드 컬럼만 `style`로 전달) + `btnSubmit`/`btnPrimary`/`btnOutline`/`btnRow`/`btnRowPrimary`. 새 관리자 화면은 반드시 이걸 재사용할 것
- 지표 계산은 `src/app/admin/metrics.ts` — MRR 시계열은 해지분도 해지 전 달까지 매출로 잡는다(`wasActiveAt`). 검증: `npx tsx metrics.test.ts`
- 스키마 추가(마이그레이션 `announcement_schedule_and_inquiry`): `Announcement.target/startsAt/endsAt`, `Inquiry` 모델
- 공지 배너 규칙 변경: "한 번에 하나만 활성" → **게시 기간 + 대상**(전체/구독/미구독 단지)으로 결정, 해당하는 것 중 최근 것 노출

### 공지 발송 대상 + 무료 체험 (2026-07-27 확정)

**무료 체험 = 모듈별 기간 한정(기본 30일, `TRIAL_DAYS` in `src/lib/modules.ts`)**
- `TenantModule.trialEndsAt` 한 컬럼: null=정식 구독 / 미래=체험 중 / 과거=만료. `SubscriptionStatus`에 `TRIAL`을 넣지 않은 이유는 기존 `status: ACTIVE` 조건이 전부 체험 단지를 조용히 빠뜨리고, 만료 전이를 돌릴 크론이 필요해지기 때문. 타임스탬프는 읽는 시점 비교로 끝난다
- 단지 상세 > 구독 모듈에서 `체험 30일` / `유료 전환` 토글. **체험은 MRR에서 제외**(`wasPaidAt`), 수입 현황에 "체험 중(잠재 매출)" KPI로 따로 표시
- 천장: 체험 이력이 안 남는다(전환 시 null로 덮어씀). 전환율 측정이 필요해지면 `trialStartedAt` 추가

**공지 대상 7종** (`src/lib/announcements.ts`, 순수 함수 — `npx tsx announcements.test.ts`)
전체 / 구독 중 / 미구독 / 무료 체험 중 / **특정 모듈 구독**(값=moduleId) / **특정 모듈 미구독**(크로스셀) / **직접 선택**(값=단지 id 목록)
- **좁은 대상이 전체 공지를 이긴다** — 한 단지에 여러 공지가 걸리면 specificity 순위로 하나만 노출(직접 선택 > 모듈 > 체험 > 구독여부 > 전체). 이전의 "최신 하나" 방식은 개별 안내가 전체 공지에 묻히는 결함이 있었음
- 값이 필요한 대상은 서버 액션에서 검증 — 값 없는 MODULE 공지는 아무에게도 안 보이므로 저장 자체를 막는다
- 세대수 구간·신규 단지·활성도 조건은 **일부러 뺐다**: 단지 수십~수백 규모에선 대상이 서너 곳으로 쪼그라들어 "직접 선택"과 결과가 같다. 단지가 수백을 넘고 직접 선택이 번거로워지면 그때 추가

### 모듈 등록 폼 (2026-07-27 정리)

- **라우트 입력 제거** — 항상 `/modules/{모듈 ID}`로 파생. 값이 바뀌지 않는 걸 입력받을 이유가 없다
- **아이콘은 자유 텍스트 → 라디오 선택** — `src/lib/module-icons.ts`에 매핑된 것만 고를 수 있게 해서 깨진 아이콘이 안 나온다. 실제 아이콘을 그려 보여주되 `peer-checked` CSS만 써서 클라이언트 컴포넌트로 만들지 않았다
- 정렬 순서 기본값 = 마지막 모듈 + 1, 각 필드에 용도 설명 한 줄씩

### 직원 계정 발급·비밀번호 재설정 (2026-07-27, 기능 검토 P1)

- 단지 상세 > 직원 카드(`staff-manager.tsx`)에서 **계정 발급**(이름·이메일·권한·직책)과 **비밀번호 재설정** 가능. 직원 0명인 단지는 발급 폼이 기본으로 열려 있음 — 신규 단지 등록 → 계정 발급이 한 화면에서 끝난다
- 임시 비밀번호는 서버에서 생성(`randomInt`, 헷갈리는 글자 0/O/1/l 제외 10자)해 **화면에 한 번만 표시** — 해시만 저장되므로 벗어나면 재발급뿐. 전달은 전화/문자로(발송 인프라 없음, 의도적)
- 재설정은 기존 비밀번호 즉시 무효화라 `confirm()` 확인 후 실행

### 가입·로그인·계정 (2026-07-27, 셀프서비스 전환 — 사용자 결정)

**가입 모델: 운영자 발급 → 셀프 가입으로 전환.** `/signup`(공개)에서 단지명 + 소장 계정(이메일·비밀번호)을 만들면 즉시 로그인된다. 이메일 인증 없음(ponytail: 가짜 가입 생기면 그때). 이전의 `/trial` 신청 폼은 삭제하고 `/signup`으로 리다이렉트. 운영자의 단지 등록·계정 발급 기능은 전화 영업 경로용으로 유지.

**무료 체험 정책 (기준 확정, 2026-07-27)**
- **단위 = 단지 × 모듈.** 직원 수와 무관 — 구독 주체가 단지라서 직원이 몇 명이든 체험은 단지당 모듈별 1회. 셀프 구독 시 자동 30일(`setSubscription` — 기존 행이 없을 때만, 재구독은 체험 없음)
- **운영자는 기간 지정 가능** — 단지 상세 > 구독 모듈에서 7/14/30/60/90일 선택해 체험 시작·기간 재설정(오늘부터 다시 계산)·유료 전환. 사용자 셀프 구독은 30일 고정
- **무한 체험 우회 = 같은 아파트의 재가입(새 단지 생성).** 단지명+주소 자동 차단은 하지 않는다 — 동명 아파트가 전국에 흔해 정당한 가입을 막고, 글자 하나 바꾸면 우회된다. 대신 **가입자 현황에서 같은 단지명(공백 무시)에 "동명 단지" 경고 표시** → 운영자가 판단해 이용 중지. 근본 대책은 이메일 인증(로드맵 5.5)

**비밀번호 재설정 위계 (사용자 결정): 본인 → 소장 → 운영자, 최종적으로는 이메일 셀프 재설정.**
- 본인: 설정 > 내 계정에서 변경 (현재 비밀번호 확인)
- 잊었을 때: **소장이 설정 > 직원 관리에서 재설정**(`directorResetStaffPassword`, 열쇠 아이콘) — 임시 비밀번호 화면 1회 표시(`src/components/temp-password-notice.tsx` 공용). 소장 본인이 잊으면 운영자(관리자 콘솔 > 단지 상세)
- 이메일 셀프 재설정은 로드맵 5.5(이메일 인증·발송 인프라)로 계획 확정 — 도입하면 소장·운영자 수동 재설정이 필요 없어짐

**로그인 페이지**: 아이디·비밀번호 찾기 접이식 안내, 카카오 로그인, "무료로 시작하기" → `/signup`
**카카오 로그인**: `/api/auth/kakao` → `/callback`. `User.kakaoId` → 이메일 매칭 최초 1회 연결 → 없으면 "등록된 계정 없음"(카카오로는 가입 불가 — 가입은 `/signup`). **`KAKAO_REST_API_KEY` 없으면 버튼 숨음** — Redirect URI `{origin}/api/auth/kakao/callback` + 동의항목 `account_email`
**내 계정**: 설정 > 내 계정 탭 + `/admin/account` — 이름·직책·비밀번호. 양쪽 사이드바 하단 프로필 클릭 진입 (`src/components/account-settings.tsx`)
기타: 헤더 검색창은 문서함 검색 명시(`Ctrl K`), 단지 상세 "직원 계정" 카드 첫 줄 배치

### 기능 검토 백로그 (2026-07-27 검토, P1은 완료)

- **P2 임퍼서네이션 감사 로그** — 진입/복귀 + 파괴적 액션(이용 중지·구독 변경) 기록. 디자인 원문의 "데이터 변경은 이력에 기록됩니다" 문구는 구현된 적 없음
- P3: 체험 만료 D-7 목록(수입 현황), 온보딩 미완료 단지(개요), 문의 답변 메모 컬럼, 공지 수정, 단지 검색(수백 단지 되면)
- 하지 말 것: 이메일/알림톡 인프라(공지문 모듈 때 묶어서), 관리자 다중 계정(운영자 1명인 동안)

### 아직 실데이터가 없는 부분

- 미수금 KPI·결제 실패 경고 — 결제 연동 전이라 "정가 기준 추정치" 안내로 대체
- 고객 문의는 단지 쪽 등록 UI 없음 — 운영자 처리 상태 관리만

## 이후 작업 — 5단계 모듈 (경쟁사 분석 반영, 2026-07-27 확정)

⚠️ **순서만 바뀐 게 아니라 범위가 재정의된 모듈이 많다.** 착수 전 해당 항목의 "재정의" 줄을 반드시 읽을 것. 조정 근거는 아래 "경쟁사 분석" 절.

### 1. 미납 독촉장 — 연체 단계 자동 에스컬레이션
- 세대 마스터에서 선택 → 미납액 입력(또는 엑셀) → 대량 생성 → `createDocument()`로 문서함 저장 + 알림
- **재정의**: 문서 1장 생성이 아니라 **연체 일수 기준 단계 진행**(1차 안내 → 2차 독촉 → 내용증명 → 지급명령 준비). 세대별 현재 단계와 다음 액션일을 추적
- 구조적 제약: 미납 원천 데이터가 ERP(이지스 등)에 있다 → **엑셀 업로드 정합성 설계가 이 모듈의 성패**

### 2. 계약서 관리 — 만료 알림 + K-apt 입찰 리드타임 역산 *(기존 3위 → 2위)*
- 만료 D-30/D-7 알림
- **추가**: 전자입찰 공고 의무(입찰서 마감 전일 기산 10일 전)를 역산해 "지금 공고 준비 시작" 알림. 만료일만 알리면 이미 늦다
- 확인 필요: K-apt 외부 연동 API 범위 (공개된 건 공고·기본정보 조회용만 확인됨)

### 3. 민원·하자 — 단계별 워크플로 + 처리기한(SLA) *(기존 4위 → 3위)*
- **재정의**: "이력"이 아니라 접수 → 배정 → 처리 → 완료 단계 + SLA 초과 알림 + 입주민 통지 기록
- 국내 무료 앱층이 접수·배정까지만 제공한다 → **그 다음 구간이 경계선**

### 4. 법정 의무 이행 증빙 — 설비 이력 + 산업보건 교육일지 통합 *(기존 5위 + 8위 병합)*
- 법정 점검주기 프리셋(소방·승강기·저수조) → 주기 도래 시 **자동 작업지시 생성** → 점검일지
- 산업안전보건 교육 등록·참석 서명·법정 기한을 같은 모듈에서
- 문서함 채번·보관과 묶어 **"지자체 감사 나와도 3분 만에 뽑는 서류철"** 로 패키징

### 5. 전자결재 *(기존 7위 → 5위)*
- "입주자대표" 권한 추가로 **기존 결재선에 통합** (별도 결재 시스템 만들지 말 것)
- 공문 출력물 "제2026-N호" 표기
- **앞당긴 이유**: 6·7번 AI 모듈이 "생성 → 결재 → 채번 → 보관" 파이프라인으로 재정의됐고, 그 파이프라인의 결재 구간이 이 모듈이다

### 6. 공지문 자동완성 (Claude API) *(기존 2위 → 6위)*
- **재정의**: 생성 자체는 무료 경쟁재(아파트케어 '아지트')가 이미 제공 → 차별점은 **생성 결과를 결재선 → 문서번호 채번 → 문서함 보관까지 흘려보내는 것**
- 알림톡 발송 옵션 검토

### 7. 회의록 자동완성 *(기존 6위 → 7위)*
- 6번과 동일 파이프라인. 입대의 의결사항 추적 확장 검토

### 5.5 이메일 인증·발송 인프라 (사용자 지정, 2026-07-27 계획 확정)
- **발송 인프라 1개**(국내 메일 발송 서비스 또는 SMTP)를 깔고 세 곳에서 재사용:
  1. **가입 이메일 인증** — 셀프 가입의 가짜 계정·무한 체험 재가입 방지의 근본 대책
  2. **비밀번호 셀프 재설정** — 재설정 링크 발송. 도입되면 소장·운영자 수동 재설정 불필요
  3. 이후 공지문 모듈의 알림톡/메일 발송과 공유
- 스키마 예상: `User.emailVerifiedAt DateTime?` + 토큰 테이블(또는 JWT 링크로 무테이블)
- 시점: 전자결재(5번) 뒤, AI 모듈(6번) 전 — 공지문 모듈이 이 인프라를 이어받는 순서

### 8. (후보) 월간 업무보고서 자동 생성
- 전 모듈 데이터 종합

### 9. (후보) 다단지 롤업 대시보드 — 위탁관리회사 본사용
- 멀티테넌트가 이미 있어 추가 비용이 낮고, **단지별 영업 대신 위탁사 단위 계약** 경로를 연다. 중소 위탁사는 자체 포털을 못 만든다

**보류 (착수하지 말 것)**
- 전자투표 — 이지스가 무료로 제공 중이라 유료화 불가
- ERP 코어(부과·수납·회계·검침)·세무 — 난이도 상 + 후발 진입 열위. **연동 파트너십이 답**

## 경쟁사 분석 (2026-07-27 조사)

### 시장 구조

국내는 네 층으로 갈라져 있고 각 층이 다른 지갑에서 돈을 받는다.
1. **ERP·회계층**(유료) — 이지스 XpERP(자사 주장 90%+ 점유), 더즌 더아파트, 야놀자 아파트테크. 부과·수납·회계·검침이 본체
2. **입주민 앱층**(관리사무소엔 무료, 광고·수수료로 수익) — 아파트너, 아파트아이, 모빌, 살다. **앱을 깔면 민원·전자결재·전자투표·공지가 공짜로 딸려온다** ← 디벅과 정면 충돌
3. **시설·장기수선 전문층** — 아파트너스/AFMS, 그리고 **아파트케어 '아지트'가 법령 RAG 챗봇 + 공고문·회의록 AI 자동작성 + 녹음 STT를 이미 무료 제공**
4. **공공** — K-apt(48개 항목 의무 공시, 전자입찰)

**핵심 함의: 뚫어야 할 벽은 기능이 아니라 "이미 무료로 깔려 있다"이다.**

### 로드맵 조정 근거 (위 5단계 목록에 반영 완료)

| 반영된 항목 | 근거 |
|---|---|
| 2번에 K-apt 입찰 리드타임 역산 추가 + 순위 상승 | 공고는 마감 전일 기산 10일 전 의무라 만료일 알림만으론 늦다. 조사 범위 내 경쟁사 중 하는 곳 없음 — 저난이도 고체감 |
| 1번을 에스컬레이션 파이프라인으로 재설계 | 1차·2차·내용증명·지급명령까지 법적 단계가 정해져 있음. PayProp·Yardi·Enumerate 공통 형태이고 이지스도 독촉을 ERP에 이미 보유 — 단발 문서면 ERP 부가기능과 구별 불가 |
| 3번을 워크플로 + SLA로 승격 | 해외 6개 제품 전원이 이 형태. 국내 무료 앱층은 접수·배정까지만 제공 — 무료 경쟁사가 안 하는 지점이 정확히 여기 |
| AI 모듈(6·7번) 순위 하락 + 파이프라인으로 재정의, 전자결재 5번으로 상승 | 아지트가 이미 무료로 공고문·회의록 생성과 녹음 STT 제공 중(월 12,000건). 생성만으로는 유료화 근거 없음 — 못 하는 건 결재·채번·보관으로 흘려보내는 것 |
| 5번+8번을 4번 "법정 의무 이행 증빙"으로 병합 | 관리사무소 전용 산업안전보건·중대재해처벌법 대응 솔루션은 조사에서 확인되지 않음 — 화이트스페이스 가능성 |
| 9번 다단지 롤업 신규 추가 | 구매 결정권자가 위탁사 본사인 경우가 많음. 우리관리는 자체 포털을 만들었지만 중소 위탁사는 못 만든다 |

### 차별화 축 (이미 가진 것)

- **ERP를 대체하지 않고 얹히는 포지션** — 관리사무소는 이지스를 못 버린다(수납 VAN·K-apt 연동). "ERP가 안 해주는 행정·문서 업무만 추가 구독"이면 교체 저항이 0
- **문서번호 자동채번 + 문서함 + 결재선 조합** — "지자체 감사 나와도 3분 만에 뽑는 서류철"로 패키징하면 구매 동기가 명확해짐
- **모듈 단위 구독 = 소규모 단지 진입 가능** — 300세대 미만 7,260곳 중 44.8%가 직원 1~3명, 소장 56.7%가 인력 부족 응답. AppFolio(50유닛 미만 거절)·Yardi Breeze(최소 $400/월)식 최소요금 장벽이 없음
- **국내 전원 가격 비공개** — 공개 요금표 + 무료 체험을 국내에서 처음 하면 그 자체가 마케팅 자산 (체험 기능은 이번에 구현됨)
- **판매 채널**: 중소 위탁관리회사 본사. 멀티테넌트가 이미 있으니 **다단지 롤업 대시보드 하나만 얹으면** 단지별 영업 대신 위탁사 단위 계약 가능

*확인 안 된 것: 국내 전 제품의 구체 가격·요금제(전원 비공개), 국내 경쟁사의 문서번호 채번 보유 여부, K-apt 외부 연동 API 제공 범위. 점유율 수치는 모두 자사 발표.*

## 개발 환경

- Next.js 16(App Router) + TypeScript + Tailwind v4 + shadcn/ui, PostgreSQL 18 + Prisma 7(`prisma-client` 생성기, `@prisma/adapter-pg`, DB URL은 `prisma.config.ts`)
- 실행: `npm run dev` → http://localhost:3000
- 데모 계정 (비밀번호 모두 `test1234`): `test1@test.com`(소장) / `test2@test.com`(경리) / `test3@test.com`(직원) / `admin@test.com`(관리자)
- 스모크 테스트: `npx tsx smoke.ts` (dev 서버 실행 중이어야 함, 26페이지 검사)
- 카카오 로그인은 `.env`에 `KAKAO_REST_API_KEY`(+선택 `KAKAO_CLIENT_SECRET`)를 넣어야 활성화 — 없으면 로그인 페이지에서 버튼 숨김
- 순수 로직 검증: `npx tsx metrics.test.ts` (MRR·체험 제외) / `npx tsx announcements.test.ts` (공지 대상 판정)
- ⚠️ 스키마 변경 후 `prisma generate` 하면 dev 서버 재시작 필요 (구버전 클라이언트 캐시로 500 발생)
