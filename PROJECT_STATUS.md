# 디벅 (dibuck) — 프로젝트 진행 상황

아파트 관리사무소 직원용 모듈 구독형 SaaS.
(최종 업데이트: 2026-07-28 — 5.5 메일·5.6 결제·5.7 플랫폼 이식 완료(토스 심사 통과 후 실키만 넣으면 됨). **기안·품의 모듈 Phase 0~4 완료(초안 생성 → 결재 → 공고문 파생 → 완료보고·지출결의까지). S-APT 붙여넣기 뷰는 보류(근거는 Phase 4 절).** 아래 "기안·품의 모듈" 절부터 읽을 것. 미납 독촉장은 사용자 결정으로 이 모듈 뒤로 밀렸다)

⚠️ **모듈 라우트를 구현하기 전까지 해당 모듈은 `isActive: false`다** — 켜는 순간 요금표에 올라가고 무료 체험이 소진되기 시작한다. 구현 완료 시 `prisma/seed.ts`의 MODULES에서 해당 줄만 `true`로 바꿀 것.

**결정 완료 (2026-07-27, 사용자)**
1. 공개 랜딩(`/`) — **나중에 개발.** 지금 손대지 않는다. 요금표 공백과 "모듈 출시 알림 신청" 창구는 랜딩 작업 때 함께(5.7에서 이 항목만 뺀 이유)
2. 메일 발송 — **SMTP + nodemailer**(벤더 SDK 아님). `building-manager`의 템플릿 12종·토큰 로직을 이식
3. PG — **토스페이먼츠, 현재 심사 중.** 심사 통과 전까지 5.5를 진행
4. **가입 이메일 인증은 구현하지 않는다** (근거는 아래 5.5 절)

## 완료된 단계 (1~4단계)

### 1단계 — 디자인 시스템
- 2계층 컬러 토큰: primitive(Slate 팔레트 + #2563EB primary) → semantic(primary, success, warning, danger, info, surface 등), `src/app/globals.css`
- 디자인 원본: 클로드디자인 프로젝트 **"건물관리자 통합 대시보드"** 의 `관리사무소 대시보드.dc.html`
- 폰트: Pretendard 스택 + JetBrains Mono(숫자·날짜·문서번호), 루트 17px(쉽고 큰 UI)
- 공용 컴포넌트: shadcn/ui 14종 + DataTable(정렬·페이징), PageHeader, EmptyState, ConfirmDialog, DatePicker, FileUpload, SideNav, AppHeader

### 2단계 — 코어
- 멀티테넌트(모든 테이블 tenant_id) + 이메일 로그인(jose JWT 쿠키) + RBAC
- 모듈 레지스트리(DB): 구독 상태에 따라 사이드바 노출/잠금. 새 모듈 = 레지스트리 등록 + `src/lib/module-icons.ts` 아이콘 한 줄
- 공통 문서함: documents 단일 테이블, 한글 문서번호 자동 채번(`공지-2026-0001`), 통합 검색
- 알림 센터: 사용자별 fan-out, 헤더 미읽음 배지
- 세대 마스터(Unit): 설정 > 세대 관리에서 엑셀 업로드(샘플 파일 제공) — 모든 모듈이 재사용

### 3단계 — 사용자 대시보드
- 홈: 오늘의 할 일 위젯 4종(결재 대기·계약 만료 D-30·미처리 민원·점검 예정), 할 일 리스트, 처리 현황, 최근 문서, 모듈 바로가기
- 모듈 런처(`/modules`): 미구독은 잠금 + 구독 유도
- 설정 5탭: 단지 정보 / 세대 관리 / 직원 관리 / 결재선(가로 플로우 시각화) / 구독 관리(카드 그리드, 구독/해지 토글)

### 4단계 — 관리자 콘솔 (`/admin`)
- 지표: 가입 단지, MRR, 모듈별 구독 수(막대), 최근 가입/해지
- 단지 관리: 목록/상세, 이용 중지(로그인 차단), 모듈 활성화 토글(feature flag)
- 모듈 관리: 레지스트리 CRUD
- 공지 관리: 전 단지 배너(한 번에 하나 활성)
- 임퍼서네이션: 세션 JWT에 `impersonating` 플래그 + 관리자 userId 유지. 상단 배너 없이 **양쪽 사이드바 하단 같은 자리에서 왕복** — 관리자 콘솔 "사용자 대시보드" → 사용자 화면 "관리자 대시보드". 개요 페이지의 진입 버튼·테스트 안내 박스는 중복이라 제거함(단지 상세의 "사용자 화면으로 전환"은 유지)

## 합의된 규칙 (새 화면 작성 시 준수)

| 항목 | 규칙 |
|---|---|
| **셀프서비스 SaaS** | **디벅은 영업주도 B2B가 아니다.** 가입→체험→구독→결제→계정 관리를 사용자가 운영자 개입 없이 스스로 끝내는 게 표준 경로. 운영자 수동 기능은 예외 도구(협상·CS)로만. 정책 값(가격·체험 기간)은 코드 상수 금지, 운영자 UI 설정으로 전 가입자 동일 적용. 사람이 해주는 임시방편은 자동화(이메일 인증 5.5, 결제 PG)로 대체가 항상 우선 |
| 권한 vs 직책 | 권한(Role)은 소장/경리/직원 3종 유지, 직책(User.title)은 자유 텍스트. 직책을 enum에 추가하지 말 것 |
| 버튼 3단계 | `lg`=폼 주요 액션(저장·업로드, h-10 min-w-32) / `default`=카드 액션(구독·해지) / `sm`=테이블 행 안 |
| 타이포 (전 화면) | **공용 체계만 사용** — `text-2xl`(페이지 제목) / `text-lg`(섹션 제목) / `text-sm`(본문·표) / `text-xs`(메타·라벨) / `font-mono text-3xl`(KPI 숫자). `text-[13px]` 같은 px 하드코딩 금지 — **관리자 콘솔도 예외 없음**. 디자인 시안에서 가져오는 건 레이아웃·구성뿐 |
| 레이아웃 폭 | 테이블·목록은 전체 폭, **입력 폼은 `max-w-2xl`(≈714px)** — 라벨과 입력칸이 한눈에 묶여 보이는 한계 폭. 더 넓히면 라벨↔값 시선 이동이 길어지고 짧은 입력칸이 화면을 가로질러 늘어난다. 폭이 더 필요한 필드는 폼을 넓히지 말고 **카드 안에서 2열 그리드**로 풀 것 |
| 금액 입력 | 천단위 콤마를 보여주는 `PriceInput`(client) 사용. 서버는 `parseWon()`으로 숫자만 걷어낸다 — `Number("39,000")`은 NaN이라 안 걷으면 가격이 조용히 0원이 된다 |
| 간격 체계 | 카드 헤더 12×16px + gray-100 구분선, 본문 16px, 테이블 행 12×16px |
| 홈/헤더 | 모듈 종속 버튼 금지 — 모듈 진입은 할 일 리스트·모듈 바로가기가 담당 |
| 문서 컨벤션 | 모듈은 `createDocument()`(자동 채번+알림) 사용, 할 일 위젯은 type+status+dueDate로 집계 |
| 입대의 결재 | 5단계 전자결재 때 "입주자대표" 권한 추가로 기존 결재선에 통합 (별도 시스템 금지) |

## 관리자 콘솔 고도화 — 완료 (2026-07-27)

디자인 원본: 클로드디자인 **"Copy of 건물관리자 통합 대시보드"** 의 `관리사무소 대시보드.dc.html`
(관리자 콘솔 화면은 이 파일에만 있음 — v2·v3·v4는 사용자 홈 시안이라 관리자 영역 없음)

### 메뉴 6개 (디자인 `adminTabs` 그대로)

| 메뉴 | 경로 | 내용 |
|---|---|---|
| 개요 | `/admin` | 사용자 대시보드 테스트 카드, KPI 4(가입 단지·MRR·모듈 구독·이번 달 해지), 단지 관리 표, 모듈별 구독 바 |
| 가입자 현황 | `/admin/tenants` | KPI 4, 단지 목록(월 요금 포함) + 단지 등록, 최근 가입·해지 타임라인, 모듈 구독 전환율 |
| 수입 현황 | `/admin/revenue` | KPI 4(MRR·ARR·단지당 평균·활성 구독), 월별 MRR 추이 7개월 막대, 모듈별 수입 |
| 모듈 관리 · 가격 | `/admin/modules` | 인라인 가격 편집 + 행별 "가격 저장", 모듈명 클릭 시 전체 수정 |
| 공지 관리 | `/admin/announcements` | 대상·게시 기간·상태(예약/게시중/종료) 표, 새 공지 작성 |
| 고객 문의 | `/admin/support` | 단지·유형·상태 표, 답변 완료 토글. 사이드바에 답변 대기 배지 |
| 작업 이력 | `/admin/logs` | (5.7 추가) 운영자 작업 감사 로그 |

### 적용 규칙

- **타이포·버튼은 사용자 대시보드와 동일한 공용 체계**(rem, 루트 17px). 관리자 화면도 `text-2xl`(페이지 제목) / `text-lg`(섹션 제목) / `text-sm`(본문·표) / `text-xs`(메타·라벨) / `font-mono text-3xl`(KPI 숫자)를 쓰고, 버튼은 공용 `buttonVariants`의 3단계(lg=폼 주요 액션 / default=페이지·카드 액션 / sm=표 행)를 그대로 위임한다. 디자인에서 가져온 것은 **레이아웃·구성**(메뉴 6개, 2fr:1fr 그리드, 표 컬럼, 섹션 구성)뿐
- 공용 프리미티브는 `src/app/admin/ui.tsx`: `PageTitle` `Section` `Kpi` `Pill` `Bar` `TwoLine` + `tableHead`/`tableRow`(그리드 컬럼만 `style`로 전달) + `btnSubmit`/`btnPrimary`/`btnOutline`/`btnRow`/`btnRowPrimary`. 새 관리자 화면은 반드시 이걸 재사용할 것
- 지표 계산은 `src/app/admin/metrics.ts` — MRR 시계열은 해지분도 해지 전 달까지 매출로 잡는다(`wasActiveAt`). 검증: `npx tsx metrics.test.ts`
- 스키마 추가(마이그레이션 `announcement_schedule_and_inquiry`): `Announcement.target/startsAt/endsAt`, `Inquiry` 모델
- 공지 배너 규칙 변경: "한 번에 하나만 활성" → **게시 기간 + 대상**(전체/구독/미구독 단지)으로 결정, 해당하는 것 중 최근 것 노출

### 공지 발송 대상 + 무료 체험 (2026-07-27 확정)

**무료 체험 = 모듈별 기간 한정(기본 30일, `TRIAL_DAYS` in `src/lib/modules.ts`)**
- `TenantModule.trialEndsAt` 한 컬럼: null=정식 구독 / 미래=체험 중 / 과거=만료. `SubscriptionStatus`에 `TRIAL`을 넣지 않은 이유는 기존 `status: ACTIVE` 조건이 전부 체험 단지를 조용히 빠뜨리고, 만료 전이를 돌릴 크론이 필요해지기 때문. 타임스탬프는 읽는 시점 비교로 끝난다
- 단지 상세 > 구독 모듈에서 `체험 30일` / `유료 전환` 토글. **체험은 MRR에서 제외**(`wasPaidAt`), 수입 현황에 "체험 중(잠재 매출)" KPI로 따로 표시
- 천장: 체험 이력이 안 남는다(전환 시 null로 덮어씀). 전환율 측정이 필요해지면 `trialStartedAt` 추가

**공지 대상 7종** (`src/lib/announcements.ts`, 순수 함수 — `npx tsx announcements.test.ts`)
전체 / 구독 중 / 미구독 / 무료 체험 중 / **특정 모듈 구독**(값=moduleId) / **특정 모듈 미구독**(크로스셀) / **직접 선택**(값=단지 id 목록)
- **좁은 대상이 전체 공지를 이긴다** — 한 단지에 여러 공지가 걸리면 specificity 순위로 하나만 노출(직접 선택 > 모듈 > 체험 > 구독여부 > 전체). 이전의 "최신 하나" 방식은 개별 안내가 전체 공지에 묻히는 결함이 있었음
- 값이 필요한 대상은 서버 액션에서 검증 — 값 없는 MODULE 공지는 아무에게도 안 보이므로 저장 자체를 막는다
- 세대수 구간·신규 단지·활성도 조건은 **일부러 뺐다**: 단지 수십~수백 규모에선 대상이 서너 곳으로 쪼그라들어 "직접 선택"과 결과가 같다. 단지가 수백을 넘고 직접 선택이 번거로워지면 그때 추가

### 모듈 등록 폼 (2026-07-27 정리)

- **라우트 입력 제거** — 항상 `/modules/{모듈 ID}`로 파생. 값이 바뀌지 않는 걸 입력받을 이유가 없다
- **아이콘은 자유 텍스트 → 라디오 선택** — `src/lib/module-icons.ts`에 매핑된 것만 고를 수 있게 해서 깨진 아이콘이 안 나온다. 실제 아이콘을 그려 보여주되 `peer-checked` CSS만 써서 클라이언트 컴포넌트로 만들지 않았다
- 정렬 순서 기본값 = 마지막 모듈 + 1, 각 필드에 용도 설명 한 줄씩

### 직원 계정 발급·비밀번호 재설정 (2026-07-27, 기능 검토 P1)

- 단지 상세 > 직원 카드(`staff-manager.tsx`)에서 **계정 발급**(이름·이메일·권한·직책)과 **비밀번호 재설정** 가능. 직원 0명인 단지는 발급 폼이 기본으로 열려 있음 — 신규 단지 등록 → 계정 발급이 한 화면에서 끝난다
- 임시 비밀번호는 서버에서 생성(`randomInt`, 헷갈리는 글자 0/O/1/l 제외 10자)해 **화면에 한 번만 표시** — 해시만 저장되므로 벗어나면 재발급뿐. 전달은 전화/문자로(발송 인프라 없음, 의도적)
- 재설정은 기존 비밀번호 즉시 무효화라 `confirm()` 확인 후 실행

### 가입·로그인·계정 (2026-07-27, 셀프서비스 전환 — 사용자 결정)

**가입 모델: 운영자 발급 → 셀프 가입으로 전환.** `/signup`(공개)에서 단지명 + 소장 계정(이메일·비밀번호)을 만들면 즉시 로그인된다. 이메일 인증 없음(ponytail: 가짜 가입 생기면 그때). 이전의 `/trial` 신청 폼은 삭제하고 `/signup`으로 리다이렉트. 운영자의 단지 등록·계정 발급 기능은 전화 영업 경로용으로 유지.

**무료 체험 정책 (기준 확정, 2026-07-27)**
- **단위 = 단지 × 모듈.** 직원 수와 무관 — 구독 주체가 단지라서 직원이 몇 명이든 체험은 단지당 모듈별 1회. 셀프 구독 시 자동 시작(`setSubscription` — 기존 행이 없을 때만, 재구독은 체험 없음)
- **표준 기간은 모듈 관리·가격에서 모듈별 설정**(`Module.trialDays`, 기본 30, 0 = 체험 없음) — 새로 구독하는 **전 단지에 동일 적용**. 가격과 체험 기간은 둘 다 상품 정책이라 같은 화면(인라인 저장)
- **단지 상세의 기간 지정(7~90일)은 개별 영업·협상용 예외 도구** — 특정 단지에만 다르게 줄 때. 표준을 바꾸려면 모듈 관리에서
- **무한 체험 우회 = 같은 아파트의 재가입(새 단지 생성).** 단지명+주소 자동 차단은 하지 않는다 — 동명 아파트가 전국에 흔해 정당한 가입을 막고, 글자 하나 바꾸면 우회된다. 대신 **가입자 현황에서 같은 단지명(공백 무시)에 "동명 단지" 경고 표시** → 운영자가 판단해 이용 중지. 근본 대책은 이메일 인증(로드맵 5.5)

**비밀번호 재설정 위계 (사용자 결정): 본인 → 소장 → 운영자, 최종적으로는 이메일 셀프 재설정.**
- 본인: 설정 > 내 계정에서 변경 (현재 비밀번호 확인)
- **메일 셀프 재설정(5.5에서 구현)** — 로그인 화면 "비밀번호를 잊으셨나요?" → `/forgot-password`. SMTP 미설정이면 이 경로가 숨고 아래 수동 경로만 안내된다
- 메일을 못 받을 때: **소장이 설정 > 직원 관리에서 재설정**(`directorResetStaffPassword`, 열쇠 아이콘) — 임시 비밀번호 화면 1회 표시(`src/components/temp-password-notice.tsx` 공용). 소장 본인이면 운영자(관리자 콘솔 > 단지 상세). **셀프 재설정이 생겨도 이 경로는 지우지 않는다**

**로그인 페이지**: 아이디·비밀번호 찾기 접이식 안내, 카카오 로그인, "무료로 시작하기" → `/signup`
**카카오 로그인**: `/api/auth/kakao` → `/callback`. `User.kakaoId` → 이메일 매칭 최초 1회 연결 → 없으면 "등록된 계정 없음"(카카오로는 가입 불가 — 가입은 `/signup`). **`KAKAO_REST_API_KEY` 없으면 버튼 숨음** — Redirect URI `{origin}/api/auth/kakao/callback` + 동의항목 `account_email`
**내 계정**: 설정 > 내 계정 탭 + `/admin/account` — 이름·직책·비밀번호. 양쪽 사이드바 하단 프로필 클릭 진입 (`src/components/account-settings.tsx`)
기타: 헤더 검색창은 문서함 검색 명시(`Ctrl K`), 단지 상세 "직원 계정" 카드 첫 줄 배치

### 기능 검토 백로그 (2026-07-27 검토, P1은 완료)

- ~~P2 임퍼서네이션 감사 로그~~ → **5.7에서 완료** (`/admin/logs`)
- ~~P3 체험 만료 D-7 목록 / 온보딩 미완료 단지~~ → **5.7에서 완료** (수입 현황의 이탈 위험 목록 / 홈의 시작 가이드)
- 남은 P3: 문의 답변 메모 컬럼, 공지 수정, 단지 검색(수백 단지 되면)
- 하지 말 것: 관리자 다중 계정(운영자 1명인 동안)

### 셀프서비스 정합 점검 — B2B 잔재 제거 (2026-07-27)

전수 점검 결과 "돈 내는 순간"과 "문제 생긴 순간"이 운영자 수동이었음. 세 가지를 고침:

**① 체험 만료 잠금 (결제 연동 전 인터림)**
- `getModulesForTenant`·`isSubscribed`: `trialEndsAt`이 과거면 `subscribed: false` + `trialExpired: true` → 사이드바·런처 잠금, 모듈 페이지 진입 차단. 이전엔 만료돼도 계속 무료 사용됐음
- `setSubscription` 가드: 만료된 모듈은 구독 토글로 재활성화 불가(잠금 우회 방지) — 유료 전환은 문의로
- **`wasPaidAt` 재정의: `trialEndsAt === null`(전환 완료)만 매출.** 만료 미전환은 결제된 적이 없으므로 MRR에서 제외 — 이전엔 만료되는 순간 유령 매출로 잡혔음. 새 헬퍼 `wasExpiredTrialAt` → 수입 현황에 "전환 대기 n건" 표시(운영자 수동 전환 목록)
- 만료·D-7 안내는 (app) 레이아웃 배너로 — 크론·알림 fan-out 없음(접속 시점 계산). 5.6 결제 연동 때 카드 등록 유도로 교체
- 검증: `metrics.test.ts` 갱신 + 만료 상태 실동작 확인(모듈 차단·배지·배너)

**② 공개 랜딩 + 요금표 (`/`)**
- 사용자 홈을 `/home`으로 이동, `/`는 비로그인 공개 랜딩(로그인 상태면 `/home`·`/admin`으로). 요금표는 모듈 레지스트리를 그대로 렌더 — 가격·체험 기간 하드코딩 없음
- `/` 참조 전부 `/home`으로 변경: side-nav, app-header 타이틀, login/signup/카카오/임퍼서네이션 redirect, `requireRole`
- ⚠️ 판매 중인 모듈이 0개라 운영 배포 시 요금표가 빈 채로 나온다(미구현 모듈 8개를 `isActive: false`로 내린 부작용, 개발에서는 데모 시드가 3개를 켜 두어 안 보인다). **결정: 랜딩은 나중에 개발한다 — 지금 손대지 않는다.** 5.7에서 "모듈 출시 알림 신청" 창구를 만들 때 요금표 자리의 대체 블록으로 함께 처리하면 모듈이 켜지는 순간 정상 요금표로 자동 복귀한다

**③ 고객 문의 등록 UI (`/support`)**
- 사이드바 "고객 문의" + 문의 폼(유형 라디오·내용·연락처) + 내 문의 내역·상태. `?category=구독` 프리셀렉트 — 체험 만료 배너·구독 관리의 "유료 전환 문의"가 여기로 연결
- `Inquiry` 스키마 변경 없음. 답변 본문 컬럼은 없음 — 답변은 전화/이메일, 상태만 완료 처리(기존 의도 유지)

남은 B2B 잔재(로드맵으로 해소): 비밀번호 셀프 재설정(5.5), 결제 자동화(5.6), 카카오 신규 가입(5.6 이후). **셀프 탈퇴는 아래 전수 점검에서 구현 완료.**

### 전수 버그·보안 점검과 수정 (2026-07-27)

코드 전체를 인증·구독/과금·업무기능·관리자 네 영역으로 나눠 점검하고 발견 30건을 고쳤다.

**세션이 이제 DB를 신뢰한다 (`src/lib/auth.ts`)** — 가장 큰 구조 변경.
- `getSession`은 JWT 검증 후 **DB를 한 번 더 조회**한다(React `cache()`로 요청당 1회). 이전에는 7일짜리 토큰만 믿어서 계정 삭제·권한 강등·단지 이용 중지가 전부 무시됐다("이용 중지"는 신규 로그인만 막는 상태였다)
- 권한(`role`)·소속(`tenantId`)·이름은 항상 DB가 정답. 삭제된 계정과 `SUSPENDED` 단지는 세션 자체가 무효
- **임퍼서네이션은 예외** — 토큰의 권한(단지 DIRECTOR)이 DB(SUPER_ADMIN)와 다른 게 정상이라, 발급자가 아직 운영자인지만 확인하고 토큰 값을 쓴다
- `User.passwordChangedAt` + JWT `iat` 비교로 **비밀번호 변경이 기존 세션을 실제로 끊는다**. `iat` 없는 토큰은 거부(fail closed) — 배포 시 기존 쿠키가 한 번 로그아웃된다
- ⚠️ 마이그레이션 함정: `DEFAULT CURRENT_TIMESTAMP` 백필은 `timestamptz → timestamp(3)` 변환 때 **DB 세션 시간대(KST) 벽시계**를 넣는다. Prisma가 그걸 UTC로 읽어 전원이 9시간 미래가 되고 통째로 로그아웃됐다. `..._fix_password_changed_at_backfill`이 `createdAt` 기준으로 되돌린다. **앞으로 시각 컬럼을 추가할 때 DB 기본값으로 백필하지 말 것**

**보안**
- 카카오 콜백이 `is_email_valid`·`is_email_verified`를 확인한다. 이전엔 미인증 이메일로 남의 계정에 연결돼 **계정 탈취가 가능**했다
- `/admin` 페이지 11곳에 `requireAdmin()` 개별 호출. 레이아웃은 인증 경계가 아니다(Next.js 인증 가이드 "Layouts and auth checks") — 소프트 내비게이션에서 다시 실행되지 않는다
- 로그인·비밀번호 확인에 시도 제한(`src/lib/rate-limit.ts`, 계정당 10분 10회). ponytail: 프로세스 메모리 — 인스턴스를 늘리면 Redis로
- `xlsx`를 SheetJS 공식 배포판 0.20.3으로 교체(npm 최신 0.18.5는 프로토타입 오염·ReDoS 미패치). `npm install https://cdn.sheetjs.com/...` 형태라 `package.json`의 URL 의존성은 의도된 것
- 이메일을 `normalizeEmail()`로 통일. `@unique`가 대소문자를 구분해 `Kim@x.com`으로 가입하면 `kim@x.com`으로 로그인이 안 됐다

**돈 경로**
- `toggleTenantModule`이 **해지할 때도** `trialEndsAt`을 지워서, 만료 잠긴 모듈을 껐다 켜면 무료 정식 구독이 되고 지난 체험 기간이 유료 매출로 소급됐다 → 켜는 경로에서만 지운다
- 단지 상세의 만료 체험이 "유료"로 표시되고 월 요금에 합산되면서 **정작 전환 버튼이 없었다**(전환 대기 건에 전환 UI가 없는 상태). `moduleState()` 3상태(paid/trial/expired)로 분리하고 만료 건에 "전환 대기" 배지 + 유료 전환 버튼을 붙였다. 단지 목록의 월 요금도 같은 기준(`isPaid`)으로 수정
- `setSubscription`이 `Module.isActive`를 확인한다 — 서버 액션 직접 호출로 판매 중단 모듈에 새 체험을 받을 수 있었다
- 스스로 해지한 모듈에 "체험 종료 잠김" 배너가 영구히 뜨던 문제(`modules.ts`의 `expired`가 `status`를 무시)

**데이터 정합**
- 문서번호: `count+1` → **최대 번호+1** + `@@unique([tenantId, docNo])` + 충돌 시 재채번. count 방식은 문서를 하나 지우면 이미 쓴 번호를 다시 발급했다
- 세대 엑셀 업로드를 `$transaction`으로 감쌌다. 전체 삭제 후 행별 upsert 중 실패하면 세대 마스터가 통째로 날아갔다. 2만 행 상한 + `createMany` 배치, "가동/나동" 행이 헤더로 오인돼 버려지던 판정도 수정
- `removeStaff`가 `tenant.approvalLine`에서 삭제된 직원을 빼낸다(유령 결재자)
- 홈 KPI·처리율이 `take: 10`한 배열 길이를 써서 37건이 10건으로 보이고 처리율이 부풀려졌다 → `count()` 사용
- 공지 배너의 `take: 5`를 제거. 최신순으로 먼저 자르면 "좁은 대상이 전체 공지를 이긴다" 규칙이 깨졌다(순수 함수는 정상, 호출부 결함)
- **날짜를 KST로 통일**(`ymdKst`/`mdKst`/`dayKst`/`kstDayStart`/`kstDayEnd` in `src/lib/utils.ts`). `toISOString().slice(0,10)`은 오전 9시 이전에 하루가 밀렸고, 공지 게시 종료일은 당일 오전 9시에 사라졌다. 검증: `npx tsx dates.test.ts`

**셀프서비스**
- **셀프 탈퇴 구현**(`deleteMyTenant`) — 설정 > 내 계정에서 소장이 단지명+비밀번호를 확인 입력하면 단지와 모든 데이터를 삭제하고 로그아웃. 개인정보보호법상 삭제 요구 대응은 선택이 아닌데 경로가 아예 없었다. Tenant 자식 관계에 cascade가 없어 트랜잭션에서 순서대로 지운다
- **시드를 부트스트랩/데모로 분리** — 운영 최초 관리자는 `ADMIN_EMAIL`·`ADMIN_PASSWORD`(12자 이상)로 만들고, 데모 데이터는 `NODE_ENV=production`이면 실행되지 않는다. 이전엔 `admin@test.com`/`test1234`와 가짜 MRR 5만원이 운영 DB에 들어갈 수 있었고, 그 시드 말고는 최초 관리자를 만들 방법도 없었다
- **미구현 모듈 8개를 `isActive: false`로** — 요금표(`/`)·구독 관리·사이드바가 전부 이 플래그를 읽는다. 자리표시 페이지뿐인 모듈을 팔면 30일 체험이 없는 소프트웨어를 상대로 소진되고 가입자는 "체험 종료 → 잠김"으로 끝난다. **모듈 라우트를 구현할 때 해당 줄을 `true`로 바꿀 것**(개발 시드는 데모용 3개만 켠다)
- **`isActive: false` = 신규 판매 중단이지 기존 구독 회수가 아니다** (`getModulesForTenant`). 처음엔 목록에서 그냥 뺐는데, 그러면 이미 쓰던 단지가 모듈에 못 들어가고 **해지 버튼도 사라지는데 `TenantModule` 행은 ACTIVE로 남아** 운영자 화면에서는 요금이 계속 잡히는 불일치가 생긴다. 지금 규칙: 구독 중이면 판매 중단이어도 계속 노출(`retired: true`, "판매 중단" 배지 + 해지 가능, 해지하면 그때 목록에서 사라지고 재구독 불가), 미구독이면 감춘다. 공개 요금표는 `isActive`만 본다. 어드민 단지 상세는 판매 중단 모듈도 계속 보여 준다(요금 계산과 어긋나지 않게 "판매 중단" 표시). 검증: `npx tsx modules.test.ts` (DB 필요)
- 알림 개별 읽음 처리(`openNotification`) — 알림을 다 눌러도 헤더 배지가 안 줄었다
- 정지 안내 문구를 "관리사무소로 문의"에서 운영팀으로(사용자가 곧 관리사무소다), 어드민의 낡은 문구 2곳 수정, 문의 목록에 연락처 상시 노출

**남긴 것**: 다단지 롤업(로드맵 9번)과 충돌하는 `User.email` 전역 unique는 **9번 착수 때 결정**(5.5에서 이메일 인증을 빼면서 선결 조건이 아니게 됐다). 모듈 가격 변경이 기존 구독자에게 즉시 소급되는 문제는 5.6 `Billing` 스키마에서 가격 스냅샷으로. 데이터 내보내기는 미구현(PIPA는 삭제만 요구).

### 아직 실데이터가 없는 부분

- 미수금 KPI·결제 실패 경고 — 결제 연동 전이라 "정가 기준 추정치" 안내로 대체
- 고객 문의 답변 본문 저장 없음 — 답변은 전화/이메일로, 화면에는 상태만

## 기안·품의 모듈 (진행 중, 2026-07-27 착수 — 사용자 결정으로 미납 독촉장보다 선행)

**설계 원본: `D:\01. claude code\minutes\`** (올림 프로젝트 — PROJECT-OLLIM.md, sapt-14-templates.md, ollim-mvp-mockup.html). 사실상 로드맵 5번(전자결재)+6번(AI 파이프라인)의 선행 구현 — **별도 모듈 id를 만들지 않고 기존 `approvals`를 "기안·품의" 33,000원(단일가, 티어 없음 — 사용자 확정)으로 재정의**했다. 여기서 만든 결재 엔진(`src/lib/gian/approval.ts`)은 이후 공지문·회의록 모듈이 재사용한다.

| Phase | 내용 | 상태 |
|---|---|---|
| 0 | 스키마(sealImage·externalApprovers·Document.meta·ApprovalStep) + 법률 규칙 엔진(`src/lib/gian/rules.ts`) + 설정 UI(직인 업로드·외부 결재자) | ✅ `7a969d9` |
| 1 | AI 초안 생성 — claude-sonnet-5 + structured outputs + 프롬프트 캐싱, 5필드 폼 + 실시간 판정 배지, A4 인쇄 | ✅ `fdfa380` |
| 2 | 결재 프로세스 — 상신·순차 승인·반려·재상신, 외부(회장·감사) 토큰 서명 링크 `/approve/[token]`, 메일 발송(SMTP 시) | ✅ `8864db6` |
| — | **실 API 검증** — 3종 생성 확인 후 프롬프트 수정 | ✅ `5b9fb99` |
| 3 | 결재 완료 시 입주민 공고문 자동 파생 (승인 전에는 만들지 않는다) | ✅ 아래 절 |
| 4 | 후속 파이프라인 — 완료보고서·지출결의서 meta 상속 | ✅ 아래 절 |
| — | S-APT 붙여넣기 뷰 | **보류** (아래 절) |

**실 API 검증 결과 (2026-07-27, 3종 생성):** 서식별 절 구성·개조식·붙임/"끝."·견적 최저가 `(선정)` 표기 모두 원문과 일치. 금액·계약방식은 코드가 준 문자열 그대로 들어간다. 입력에 없는 수치는 지어내지 않고 `needsClarification`으로 넘기며, 불확실한 법령 조항은 "조항 확인 필요"로 자진 신고한다. 여기서 나온 수정 2건:
- **오늘 날짜 주입** — 모델이 연도를 몰라 `○○년 8월`로 비웠다. 날짜는 volatile이라 캐시되는 `system`이 아니라 **user turn**에 넣는다(system에 넣으면 매일 프롬프트 캐시가 통째로 깨진다).
- **장충금 경고 오탐 제거** — 예산 0원 문서(점검 안내)에도 "수선유지비로 집행하면 과태료" 경고가 떴다. 집행이 없으니 오탐이고, 무의미한 경고가 쌓이면 정작 필요한 경고까지 무시하게 된다. 판정을 `isLtp` → `docType === "ltp_work"`(예산 있을 때만)로. 오탐 케이스는 `gian-rules.test.ts`에 박아 뒀다.

### Phase 3 — 입주민 공고문 자동 파생 (2026-07-28 완료)

마지막 결재자가 승인하면 `actOnStep`이 `status: "final"` 직후 공고문을 만든다(`src/lib/gian/notice.ts`). 원본 `meta.form`을 그대로 상속하므로 **사용자 입력을 다시 받지 않는다.**

- **문구는 전부 결정적 코드 — LLM을 다시 부르지 않는다.** 공고문은 정형 문구라 작문이 필요 없고, 결재 완료가 API 실패로 막히면 안 된다. 같은 이유로 파생 실패는 `try/catch`로 삼키고 **결재 완료를 롤백하지 않는다** — 공고문은 다시 만들 수 있지만 결재 완료가 풀리면 결재자 전원이 다시 승인해야 한다
- 삼킨 실패의 복구 경로는 **문서 화면의 "입주민 공고문 만들기" 버튼**(`makeGianNotice`, 완료 문서에서만 노출). 운영자 문의로 떠넘기지 않는다(셀프서비스 규칙)
- **예산 없는 기안(점검·행사 안내)에는 공사 문구를 붙이지 않는다** — 라벨이 "공사일자"에서 "시행일자"로 바뀌고 "통행·주차 제한" 유의사항이 빠진다. Phase 2의 장충금 경고 오탐과 같은 판단
- 저장: `createDocument({ type: "notice", moduleId: "approvals", meta: { sourceDocId, notice } })` — 채번은 기존 "공지" 접두사. **중복 방지·역링크가 같은 조회**(`findNoticeFor`, `meta.sourceDocId` JSON 필터)라 양방향 링크가 저절로 맞는다
- 렌더러 `src/components/notice-paper.tsx`(목업 v7 양식 이식, A4 210×297mm). 라우트는 `/modules/approvals/[docId]`를 **그대로 재사용** — `meta.notice`가 있으면 공고문으로 분기한다. 문서함·모듈 목록의 기존 링크가 그대로 동작한다
- **명의·연락처·직인은 스냅샷하지 않고 렌더 시점 `Tenant` 값을 읽는다**(연락처는 최신이 맞다). 이 때문에 `Tenant.phone`·`fax` 컬럼을 추가했다(마이그레이션 `tenant_contact`, 설정 > 단지 정보). 직인이 없으면 "(직인생략)", 번호가 없으면 TEL 줄이 빠지고 안내가 뜬다
- `PrintStyle`에 `margin` prop 추가 — 공고문은 자체 여백(14/15mm)이 있어 `@page margin: 0`
- 검증: `npx tsx gian-notice.test.ts`(문구·조사·오탐, DB 불필요) + `approval-run.test.ts`에 파생 1건·역링크·중복 방지 추가
- ⚠️ **A4 인쇄 레이아웃은 아직 눈으로 확인하지 않았다** — 브라우저 스크린샷 수단이 없었다. 데모 단지에 공고문 1건을 넣어 뒀으니 dev 서버를 **재시작한 뒤**(Prisma 클라이언트가 갱신돼야 한다) 확인할 것

### 목업 디자인 적용 + 문서 다듬기 (2026-07-28)

`D:. claude code\minutes\ollim-mvp-mockup.html`(올림 목업 v7)의 **화면 구성·문서 양식**을 이식했다.
**색과 레이아웃만 목업에서 가져오고, 글자 크기·버튼은 프로젝트 공용 체계를 쓴다**(사용자 지시로 한 번 목업 px를 그대로 썼다가 되돌림).

- **문서용 팔레트만 따로**: `globals.css`의 `--gian-*`(paper #FAF9F5 / navy #1E4066 / stamp #B3382C / doc-line #8A94A0 …). 공문서 화면은 앱 UI가 아니라 "종이"라 색이 다른 게 맞다. **글자 크기는 앱 공용(text-sm/text-xs), 버튼은 공용 `Button`** — 이 모듈만의 버튼 클래스를 만들지 말 것
- **문서 양식**(`gian-paper.tsx`): 문서번호·시행일자·수신 + 결재란(세로 "결재" 캡션) → 중앙 대제목 + 밑줄 → `1. 관련 근거` → 절 → 붙임 → 마무리 문구 + 명의. props(문서번호·명의·유형·작성일)라 외부 서명 페이지도 같은 문서를 보여준다
  - **크기는 pt로 고정** — 종이 위 크기가 기준이라 px가 아니다. 본문 11.5pt(공문서 관행 11~12pt), 절 제목 12pt, 대제목 17pt, 머리말 9.5pt. A4 인쇄 폭은 210-32=178mm
  - **`whitespace-pre-wrap`** — `가. 공 사 명:` 처럼 라벨을 공백으로 맞춘 표기가 브라우저에서 뭉개지지 않게. 이게 없으면 S-APT 서식의 자간 정렬이 전부 사라진다
  - **붙임은 라벨/항목 2열** — 번호(1. 2. 3.)가 라벨 오른쪽 같은 위치에서 시작하고, 줄이 길어지면 `-indent-5 pl-5`로 다음 줄도 그 위치를 지킨다
  - **개요 절은 표로** — 한 절에 `가. 라벨: 값` 형태가 3줄 이상이면 목업 정보표(.kv)로 그린다. 서술형 절은 개조식 그대로. 표에 안 들어가는 하위 항목·※ 단서는 표 아래에 남긴다
  - **결재선이 없어도 결재란은 나온다** — 미설정이면 공란 3칸(담당·검토·결재). 이전에는 칸이 통째로 사라져서 인쇄물이 결재란 없는 문서가 됐다. 생성 시 `needsClarification`에도 "결재선이 비어 있습니다"를 넣는다
- **결과 화면**: 문서 유형 인장 + 2단 그리드(문서 / 사이드 320px, gap-8). **문서는 열 안에서 가운데 정렬** — 왼쪽에 붙어 있으면 결재 패널과 붙어 보인다
- **초안 직접 수정**(`[docId]/edit`, `saveGianDraft`): 제목·관련근거·절별 heading/본문·붙임을 그대로 고친다. 줄바꿈이 항목 하나라 파서가 없다. **결재가 시작된 문서(pending·final)는 수정 불가** — 결재자가 본 문서와 달라지면 안 된다. 저장 시 문서함 검색용 `content`도 같이 갱신
- **인쇄 여백 버그 수정**: `PrintStyle`이 대상 시트의 `padding: 0`을 강제해서 **공고문의 자체 여백(14/15mm)이 인쇄에서 사라졌다.** 이제 위치만 잡고 여백은 각 문서가 정한다 — 기안·품의는 `print:p-0`(여백은 `@page` 18/16mm), 공고문은 자체 mm 여백 유지(`@page` 0)
- **전 화면 좌우 여백**: `(app)` 레이아웃 `p-6` → `lg:px-10 lg:py-8`. 사이드바에 본문이 붙어 있었다
- **서브페이지 머리 통일**: 통합 문서함과 같은 `PageHeader` 패턴을 고객 문의·새 기안·품의에 적용(설정은 이미 사용 중)
- **일부러 이식하지 않은 것**: 문서대장(모듈 목록이 담당 — 보존기한 안내 한 줄만) / 카카오 알림톡 결재 패널(미도입) / 공고문 스튜디오·온보딩·요금제(범위 밖) / 목업의 다크 팔레트(앱에 테마 토글 없음)

### Phase 4 — 후속 문서 파이프라인 (2026-07-30 완료)

결재가 끝난 품의에서 **완료보고서(서식 11)·지출결의서(서식 6)**를 잇는다. 원본 `meta`를 상속하므로 폼은 **새로 알아야 하는 값만** 받는다(공사명·계약금액·업체는 다시 묻지 않는다).

- **문구는 전부 결정적 코드**(`src/lib/gian/followup.ts`) — LLM을 부르지 않는다. 두 서식은 값을 채우는 정형 문서라 작문이 필요 없다(Phase 3 공고문과 같은 판단, 건당 비용 0원)
- **둘 다 원 품의에서 직접 파생한다** — 완료보고는 지출결의의 선행 조건이 아니다(용역 대금처럼 완료보고 없이 지출만 하는 경우가 있고, 서식 6의 "관련품의"도 품의를 가리킨다). 목업의 후속 버튼 구성도 같다
- **공고문과 달리 자동 파생이 아니다** — 공사가 실제로 끝난 뒤, 대금을 낼 때 사람이 시작한다. 결재 완료 문서의 "이어서 만들기" 카드에 버튼으로 붙고, **지출이 없는 문서(점검·행사 안내)에는 나오지 않는다**(`amountRaw > 0`)
- **결재선은 서식대로**(`followupCls`): 완료보고 3단(회장 없음) / 지출결의 4단(+회장). ⚠️ **단 원 품의가 소장 전결로 끝났으면 지출결의에도 회장을 붙이지 않는다** — 같은 지출인데 결의서에서만 회장 결재를 요구하면 전결 규정과 어긋난다. 장충금 공사(5단)의 지출결의도 서식대로 회장까지만(감사 없음)
- **파생 문서의 `context`는 `none`** — 견적서 첨부 가드(`quoteFileGap`)는 업체를 고르는 문서의 요건이다. 업체가 이미 정해진 뒤의 보고·결의에 견적서를 다시 요구하면 상신이 막힌다
- **첨부증빙 체크리스트는 만들지 않았다**(서식 6에는 있다) — 증빙은 체크박스가 아니라 실물 파일이라는 기존 결정(`cc34482`)을 따른다. 실제 첨부파일이 곧 붙임이다
- 새 화면·렌더러를 만들지 않았다: `meta.draft`를 채워 넣으므로 **문서 화면·`GianPaper`·결재 패널·초안 수정이 그대로 동작한다.** `GianPaper`에 `foot` prop만 추가(서식이 제 맺음말을 가진다). 채번 접두어 2개 추가 — `보고-`/`지출-`
- 라우트 `/modules/approvals/[docId]/followup/[kind]`(입력 폼) → `createFollowup` → 초안 저장 후 문서 화면으로. **중복 방지**: 이미 만들었으면 폼 대신 그 문서로 보낸다(`findFollowupFor`, Phase 3 `findNoticeFor`와 동형)
- 검증: `npx tsx gian-followup.test.ts`(상속·항목기호·결재선, DB 불필요) + `approval-run.test.ts`에 파생 1건(채번·역링크·3단 결재선) 추가

**S-APT 붙여넣기 뷰는 보류(2026-07-30, 사용자 판단).** 근거였던 올림의 "라이트 플랜"(외부 전자결재 단지 19,000원)이 **디벅의 33,000원 단일가 확정으로 사라졌고**, 대상이 서울 의무관리단지 일부(~2,500곳, 그나마 S-APT 실제 등록은 778건)라 지금 만들 근거가 약하다. **디벅에서 결재를 마친 뒤 S-APT에도 병행 입력하는 단지가 실제로 나타나면** 그때 항목별 복사 버튼을 붙인다(반나절 일).

**핵심 결정·규칙 (바꾸기 전에 근거부터):**
- **판정은 전부 코드**(`rules.ts`), LLM은 작문만. 예산 유무 → 기안서/품의서, 장충금 키워드+예산 → 공사 추진 기안서(5단). VAT는 /1.1 환산 후 500만 비교. 한글 금액은 변조 방지 표기(일십·일백·일천 — 목업과 다름, 의도).
- **외부 결재자(회장·감사)는 User가 아니다** — `Tenant.externalApprovers`(JSON, ETC 역할로 이사·동대표 추가 가능) + `ApprovalStep` 이름 스냅샷 + 토큰 링크(7일·1회용·재발급 시 무효, PasswordReset 규칙). 계정을 만들면 RBAC·과금이 오염된다.
- **결재선은 상신 시점 스냅샷** — 진행 중 설정 변경이 기존 결재에 영향 없음. 재상신은 스텝 전체 삭제 후 새 스냅샷.
- 이중 제출 방지는 조건부 updateMany(status: "pending" 가드) — 내부·외부 동일.
- 직인은 파일 스토리지 없이 **DB에 data URI**(1MB 제한). 스토리지가 생기면 그때 이관.
- AI: `ANTHROPIC_API_KEY` 없으면 생성만 "준비 중"(env 게이트 규칙). 단지당 일일 30건 한도(rate-limit.ts 재사용). 모델은 `claude-sonnet-5`, structured outputs로 파싱 실패가 없고, few-shot(S-APT 서식 3종)은 `cache_control`로 캐시되는 고정부다 — **여기에 날짜 같은 volatile 값을 넣지 말 것**.
- 검증: `npx tsx gian-rules.test.ts` / `approval-flow.test.ts` / `gian-notice.test.ts` / `gian-followup.test.ts`(DB 불필요) / `approval-run.test.ts`(DB 필요 — 상신→반려→재상신→순차 승인→외부 토큰→완료 전이→공고문 파생→후속 문서 파생) / `gian-draft.manual.ts`(실 API, 비용) / smoke 35페이지.

## 이후 작업 — 5단계 모듈 (경쟁사 분석 반영, 2026-07-27 확정)

⚠️ **순서만 바뀐 게 아니라 범위가 재정의된 모듈이 많다.** 착수 전 해당 항목의 "재정의" 줄을 반드시 읽을 것. 조정 근거는 아래 "경쟁사 분석" 절.

### 1. 미납 독촉장 — 연체 단계 자동 에스컬레이션
- 세대 마스터에서 선택 → 미납액 입력(또는 엑셀) → 대량 생성 → `createDocument()`로 문서함 저장 + 알림
- **재정의**: 문서 1장 생성이 아니라 **연체 일수 기준 단계 진행**(1차 안내 → 2차 독촉 → 내용증명 → 지급명령 준비). 세대별 현재 단계와 다음 액션일을 추적
- 구조적 제약: 미납 원천 데이터가 ERP(이지스 등)에 있다 → **엑셀 업로드 정합성 설계가 이 모듈의 성패**

### 2. 계약서 관리 — 만료 알림 + K-apt 입찰 리드타임 역산 *(기존 3위 → 2위)*
- 만료 D-30/D-7 알림
- **추가**: 전자입찰 공고 의무(입찰서 마감 전일 기산 10일 전)를 역산해 "지금 공고 준비 시작" 알림. 만료일만 알리면 이미 늦다
- 확인 필요: K-apt 외부 연동 API 범위 (공개된 건 공고·기본정보 조회용만 확인됨)
- **개발 프롬프트 작성 완료 (2026-08-05)** — `docs/superpowers/specs/2026-08-05-contracts-procurement-prompt.md`. 소장 고통 5위(사업자 선정 — 절차 하나 어긋나면 감사 지적, 금액 걸려 양쪽에서 의심) 대응으로 "만료 알리미"를 **"사업자 선정·계약 대장"으로 재정의**(가칭, id·20,000원 유지). 핵심 결정: 계약=Document(type `contract`+dueDate — 홈 위젯 컨벤션이 이미 전제, 별도 테이블 없음), 갱신 절차=방식별 단계 체크리스트 스냅샷(항목은 카탈로그, 완료 판정은 사람 — 지침 해석은 코드가 안 한다), 역산 엔진은 순수 함수(공고 10일은 지침 확정 값, 버퍼는 단지 설정), 수의계약 500만 판정은 `gian/rules.ts` import 재사용(복사 금지), 절차 경과 보고 A4=투명성 출력물, LLM 없음. K-apt 연동·적격심사 계산은 범위 밖(근거는 스펙에). Phase 0 = 선정지침 최신 고시 확정

### 3. 민원·하자 — 단계별 워크플로 + 처리기한(SLA) *(기존 4위 → 3위)*
- **재정의**: "이력"이 아니라 접수 → 배정 → 처리 → 완료 단계 + SLA 초과 알림 + 입주민 통지 기록
- 국내 무료 앱층이 접수·배정까지만 제공한다 → **그 다음 구간이 경계선**

### 4. 법정 의무 이행 증빙 — 설비 이력 + 산업보건 교육일지 통합 *(기존 5위 + 8위 병합)*
- 법정 점검주기 프리셋(소방·승강기·저수조) → 주기 도래 시 **자동 작업지시 생성** → 점검일지
- 산업안전보건 교육 등록·참석 서명·법정 기한을 같은 모듈에서 → **교육일지 절반은 `safety-training`으로 출시 완료**
- 문서함 채번·보관과 묶어 **"지자체 감사 나와도 3분 만에 뽑는 서류철"** 로 패키징
- **남은 절반(법정점검 대장)의 개발 프롬프트 작성 완료 (2026-08-05)** — `docs/superpowers/specs/2026-08-05-legal-inspection-prompt.md`. 새 세션에 그대로 주면 착수 가능. 핵심 결정: `facilities` 모듈 재정의(id 유지, 이름 "법정점검 대장" 가칭, 20,000원), 카탈로그(상수)→InspectionItem(앵커일)→Document(점검 기록) 3계층, 다음 도래일은 저장 없이 순수 함수 계산, **LLM 없음**(정형 문서라 결정적 코드), 크론 `/api/cron/inspection`이 작업지시 자동 생성 → 홈 할 일 위젯 "점검 예정" 연동, 감사 서류철(binder)에 교육일지 행 포함으로 병합 완성. Phase 0 = 법령 원문 재확인(스펙의 주기 표는 계획용)
- **"설비 이력"(수리 이력)은 별도 모듈로 분리 확정 (2026-08-05, 사용자 결정)** — `docs/superpowers/specs/2026-08-05-repairs-ledger-prompt.md`. 소장 고통 4위(노후 단지 수선 수요 — 인력 부족 이유 1위 63.7%) 대응. 분리 근거: 구매 동기(과태료 방지 vs 인수인계·예산 설득)와 대상(전 단지 vs 노후 단지)이 다르고, 노후 소규모 단지가 수선만 골라 구독할 수 있어야 한다. 핵심 결정: 신규 id `repairs`(가칭, 가격은 구현 시 확정), Equipment 테이블(마스터)+수선 기록=Document(`수선-` 즉시 채번 — 초안 단계 없음), 설비 미지정 수선 허용(기록 문턱 최소화), 모바일 한 손 입력 요구, 반복 고장 집계는 순수 함수, A4 설비 이력 카드=인수인계 출력물, LLM·법령 조사 없음. **Wrench 아이콘은 repairs가 가져가고 facilities 설명에서 "수리 이력"을 제거**(먼저 구현되는 세션이 수행)

### 5. 전자결재 *(기존 7위 → 5위)*
- "입주자대표" 권한 추가로 **기존 결재선에 통합** (별도 결재 시스템 만들지 말 것)
- 공문 출력물 "제2026-N호" 표기
- **앞당긴 이유**: 6·7번 AI 모듈이 "생성 → 결재 → 채번 → 보관" 파이프라인으로 재정의됐고, 그 파이프라인의 결재 구간이 이 모듈이다

### 6. 공지문 자동완성 (Claude API) *(기존 2위 → 6위)*
- **재정의**: 생성 자체는 무료 경쟁재(아파트케어 '아지트')가 이미 제공 → 차별점은 **생성 결과를 결재선 → 문서번호 채번 → 문서함 보관까지 흘려보내는 것**
- 알림톡 발송 옵션 검토

### 7. 회의록 자동완성 *(기존 6위 → 7위)*
- 6번과 동일 파이프라인. 입대의 의결사항 추적 확장 검토
- **개발 프롬프트 작성 완료 (2026-08-05)** — `docs/superpowers/specs/2026-08-05-minutes-pipeline-prompt.md`. 새 세션에 그대로 주면 착수 가능. 소장 고통 2위(입대의 대응)에 대응해 "생성"이 아니라 **소집→기록→서명→공고→의결 추적 파이프라인**으로 재정의(생성만으로는 아지트 무료와 구별 불가 — 경쟁사 분석 확정 그대로). 핵심 결정: `minutes` 모듈 재정의(id 유지, 20,000원), 동대표 명부는 `externalApprovers` 재사용, 의결사항은 `Resolution` 테이블(완성 후에도 상태가 변하므로 meta 스냅샷이 아님) + 다음 회의 안건 자동 제안, **회의록 전자서명 기본 + 자필 병행**(2026-08-05 사용자 결정 — 전자서명법 제3조·공동주택관리법 제22조 근거 확인, 기존 `/approve/[token]` 엔진을 병렬 서명으로 변형, 증거력 3장치: 서명 기록·서명 후 수정 잠금·관리규약 확인 안내). STT·전자투표·본인인증은 범위 밖(근거는 스펙에)

### 5.5 메일 발송 — **구현 완료 (2026-07-27)**

`src/lib/mailer.ts`(nodemailer+SMTP) + 비밀번호 셀프 재설정. **체험 D-7·만료 메일은 5.6으로 미뤘다** — 보낼 크론이 아직 없고, 접속 시점에 보내면 접속 안 하는 사람에게 안 닿아 목적을 못 이룬다.

- **`.env`에 `SMTP_HOST`·`SMTP_USER`·`SMTP_PASS`가 없으면 셀프 재설정 UI가 통째로 숨는다**(카카오 버튼과 같은 규칙). 대신 로그인 화면과 `/forgot-password`가 "소장님께 요청 → 소장 계정이면 운영팀" 안내를 띄운다. 선택: `SMTP_PORT`(기본 465)·`SMTP_SECURE`·`SMTP_FROM`, 그리고 **메일 링크에 쓸 `APP_URL`(배포 시 필수, 기본 localhost:3000)**
- 토큰은 `PasswordReset` 테이블(토큰 자체가 PK, `crypto.randomBytes(32)`, **1시간**, `usedAt`으로 1회용). 재요청 시 기존 미사용 토큰을 무효화해 유효 링크가 하나만 살아있게 한다
- **1회용 보장은 조건부 `updateMany`**(`usedAt: null, expiresAt: gt`) — 링크를 두 번 눌러도 한 번만 잡힌다. 비밀번호 갱신과 함께 트랜잭션
- **재설정은 `passwordChangedAt`을 갱신한다** → 그 이전에 발급된 세션 토큰이 전부 무효(auth.ts의 `iat` 비교). 비밀번호를 잊은 이유가 계정 탈취일 수 있어 남의 세션을 끊는 게 핵심
- **계정 열거 차단**: 가입 안 된 이메일도, 발송이 실패해도 응답 화면이 같다(실패 원인은 서버 로그). 이메일당 10분 3회 제한
- `onDelete: Cascade` — 직원 삭제·단지 셀프 탈퇴가 재설정 행 때문에 실패하지 않게 DB에 맡겼다
- 소장·운영자 수동 재설정은 **그대로 남긴다** — 메일을 못 받는 경우의 백업
- 검증: `npx tsx password-reset.test.ts`(토큰 상태 판정, DB 불필요) + `npx tsx smoke.ts`(공개 페이지에 `/forgot-password`·`/reset-password/[token]` 추가, 28페이지)

#### 결정 배경 (범위 축소)

**"이메일 인증·발송 인프라" → "메일 발송"으로 범위를 줄였다. 가입 이메일 인증은 구현하지 않는다.**

근거 세 가지:
- 목적이 "가짜 계정·무한 체험 재가입 방지"였는데 **임시메일로 그대로 뚫린다.** 막으려는 대상을 못 막는다
- 진짜 방지책은 **카드 등록(5.6 토스)** 이다. 체험 후 카드를 안 넣으면 잠기므로 재가입해 얻는 건 모듈별 30일뿐
- **선행 프로젝트도 결국 껐다** — `building-manager`는 로그인에 `emailVerified` 게이트(`routes/auth.js:87`)를 걸어 놓고도 체험 가입에서 `emailVerified: true`로 생성(`routes/trial.js:51`)해 우회시켰다. 결과적으로 카드까지 낸 유료 가입자만 막고 정작 남용 위험인 무료 체험은 통과시키는 뒤집힌 상태였다

→ `User.emailVerifiedAt` 컬럼과 인증 토큰 테이블은 만들지 않는다. 덤으로 **`User.email` 전역 unique 결정도 5.5에서 빠진다**(다단지 롤업 9번에서 다루면 됨).

**대신 메일 발송 자체는 필요하다.** ① 비밀번호 셀프 재설정 — 지금은 소장이 잊으면 운영자 전화가 유일한 경로 ② 결제 안내(5.6 필수) — 앱 내 배너는 **접속해야** 보인다. 결제 실패는 접속 안 하는 사람에게 닿아야 한다.

**방식: SMTP + nodemailer** (Resend 등 벤더 SDK 아님). `building-manager/backend/src/utils/mailer.js`를 이식한다 — env `SMTP_HOST/PORT/USER/PASS/SECURE/FROM`, 공통 `deliver()`가 실패 시 3초 후 1회 재시도, 템플릿 12종 완비. 벤더 종속이 없어 네이버웍스·구글 워크스페이스 SMTP를 그대로 쓸 수 있다.

### 5.6 결제 자동화 (토스페이먼츠) — **구현 완료 (2026-07-27)**

체험 종료 → 카드 등록 → 자동 결제 → 실패 시 유예·재시도 → 정지 → 셀프 재결제까지 운영자 개입 없이 돈다. 이전 인터림("체험 만료 시 잠금 + 유료 전환 문의 → 운영자 수동 전환")은 제거됐다.

**크론 방식 (선결 사항 해결):** `POST /api/cron/billing` + `Authorization: Bearer $CRON_SECRET`. Next.js에는 상주 프로세스가 없어 크론을 앱 안에 둘 수 없으므로 **밖에서 때리는 라우트 하나**로 만들었다 — Vercel Cron이든 다른 스케줄러든 같은 방식으로 붙으므로 **배포처를 먼저 정하지 않아도 된다.** 하루 한 번(새벽 권장).

```
0 3 * * *  curl -X POST -H "Authorization: Bearer $CRON_SECRET" https://.../api/cron/billing
```
크론이 하는 일: ① 청구·재시도·정지 ② 갱신 예고 D-7·D-1 ③ 체험 D-7·만료 안내(5.5에서 이월). 하루에 두 번 돌아도 이중 청구되지 않는다(성공하면 `nextBillingAt`이 다음 달로 밀린다).

**크론 두 번째 — 안전보건교육 미이수 안내:** `/api/cron/training` (같은 `CRON_SECRET`, GET·POST 둘 다). 반기 마감(6/30·12/31) **D-30·D-7**에 미이수자를 인앱 알림 + 마스터 메일로 알린다. 미이수는 문서가 아니라 계산값이라 저장 시점 훅이 없어서 배치가 필요하다.

```
0 4 * * *  curl -H "Authorization: Bearer $CRON_SECRET" https://.../api/cron/training
```
두 가지를 본다: ① **정기교육** — 반기 마감 D-30·D-7, 단지 단위(직군 전원 기준) ② **채용 시 교육** — 입사 후 7일·30일, **사람 단위**라 반기 회차와 무관하게 매일 확인한다.

회차 중복은 `Notification.type`의 키(`training_due_2026H2_D30` / `training_newhire_{staffId}_D30`)로 막는다. 회차 판정이 "당일"이 아니라 "임계를 처음 넘긴 때"라 크론이 하루 빠져도 안내가 사라지지 않는다.

**채용 시 교육의 대상은 `TrainingStaff.hiredAt`으로만 정해진다 — 비어 있으면 판정에서 아예 빠진다.** 디벅 도입 전 입사자를 전부 미이수로 띄우면 알림이 그날로 무의미해지기 때문이다. 새로 뽑은 사람만 입사일을 넣으면 자동으로 대상이 되고, 마이그레이션·백필이 필요 없다. 정기교육 시간을 감하는 규정은 적용하지 않는다(법 조문 해석은 코드가 하지 않는다 — 전결 한도와 같은 원칙). 일용·1주 이하 기간제 1시간 기준도 구분하지 않는다(8시간 단일 — 많이 요구하는 쪽이라 안전한 오차).

**청구 모델: 단지당 월 1회 통합 청구.** 그 시점에 유료인 모듈 가격의 합. 체험 중인 모듈은 빠지고, 체험이 끝나는 날부터 포함된다.
- `Billing`(단지 1:1) — `customerKey`·`billingKey`·카드 표시정보·`status`·`nextBillingAt`·`billingDay`·`pastDueSince`·`cancelRequestedAt`
- `Payment` — 청구 이력. **`items`에 가격 스냅샷**을 남겨 모듈 가격이 바뀌어도 과거 내역이 소급되지 않는다
- `Billing`을 `Tenant` 컬럼으로 붙이지 않은 이유: `Tenant.status`(이용 중지)와 결제 상태는 뜻이 다른데 이름이 겹쳐 3시에 깨질 버그가 된다
- **`billingDay` 컬럼이 필요한 이유**: 매번 +1개월로 계산하면 1/31 → 2/28 → 3/28처럼 청구일이 앞으로 밀린다. 기준일을 따로 들고 그 달 말일로 잘라 쓴다

**결제 실패 처리(dunning)**: 실패 → `PAST_DUE`(서비스는 계속 사용) → **유예 7일 동안 매일 재시도** → 넘기면 `SUSPENDED`(전 모듈 잠금, 데이터는 보존) → 셀프 재결제로 즉시 복구.
- building-manager의 D+3·D+7 간헐 재시도 대신 **매일** 재시도한다 — 카드 한도·잔액은 다음 날 풀리는 일이 흔하고 재시도 비용이 0이라 회수가 잘 된다. 로직도 두 줄로 끝난다
- `pastDueSince`는 **첫 실패에만** 찍는다. 재시도마다 갱신하면 유예가 영원히 끝나지 않는다(검증 항목에 포함)

**잠금 규칙이 바뀌었다** (`src/lib/modules.ts`):
| 상태 | 결과 |
|---|---|
| 체험 만료 + 카드 없음 | 그 모듈만 잠금 → "카드 등록하고 계속 쓰기" |
| 체험 만료 + **카드 있음** | **잠기지 않는다.** 다음 청구에 포함된다 |
| `SUSPENDED` | 단지의 전 모듈 잠금. 재결제하면 즉시 풀린다 |

**일할(proration) 계산은 일부러 넣지 않았다.** building-manager는 회사 단위 체험이라 중도 추가 모듈에 체험이 없어서 일할이 필요했지만, 현 프로젝트는 **모듈을 새로 구독하면 체험이 자동으로 시작**되므로 체험 기간이 곧 유예다. ponytail: 체험을 이미 쓴 모듈을 재구독하면 다음 청구일까지 최대 한 달이 무료다 — 재구독 빈도가 의미 있게 늘면 그때 일할을 넣을 것.

**세금계산서는 이번에 빼고** 고객 문의(/support) 유형으로 처리한다. popbill 연동은 발행 건수가 쌓인 뒤에.

**화면**
- 설정 > **결제**(`/settings/billing`, 탭 신설) — 카드 등록·변경, 다음 청구 예정 금액과 항목, 결제 내역(영수증 링크), 해지 예약/철회, 유예·정지 시 "지금 결제하기"
- 카드 등록은 토스 결제창(`requestBillingAuth`) → `/api/billing/callback`에서 `authKey`를 빌링키로 교환. **카드번호는 우리 서버를 지나지 않는다**(PCI 범위 밖). 콜백은 쿼리의 `customerKey`를 믿지 않고 세션 단지의 저장값과 대조한다
- 등록 즉시 정산 — 잠긴 모듈이 있으면 그 자리에서 결제되며 풀리고, 전부 체험 중이면 체험 종료일이 첫 청구일이 된다
- (app) 레이아웃 배너에 유예·정지 경고 추가, 체험 만료 배너는 "유료 전환 문의" → "카드 등록하고 계속 쓰기"로 교체
- 수입 현황에 **미수금**(유예·정지 단지의 마지막 실패 청구액) 표시

**메일 6종 추가**(5.5 인프라 재사용): 체험 D-7 / 체험 만료 / 갱신 예고 / 결제 성공(영수증) / 결제 실패 / 정지. 안내 메일 실패가 청구를 막지 않도록 `trySend()`로 감쌌다.

**검증**: `npx tsx billing.test.ts`(청구 대상·말일 처리·윤년·크론 판정·주문번호, DB 불필요) / `npx tsx billing-run.test.ts`(무청구·실패 유예·재시도·정지·해지 예약 상태 전이, DB 필요) / `modules.test.ts`에 잠금 3종 추가 / smoke 29페이지 + 크론 라우트 401 확인

**남은 것**: 환불(토스 `/v1/payments/{paymentKey}/cancel`)은 운영자 화면에 없다 — 지금은 토스 대시보드에서 처리. 카카오 신규 가입 허용은 아직 검토 전.

### 5.7 플랫폼 이식 — **구현 완료 (2026-07-27)**

`building-manager`의 플랫폼 기능을 옮기되, **9개 후보 중 5개만 만들고 4개는 뺐다.** 뺀 것의 근거가 이 절의 절반이다 — 나중에 "왜 안 했지?" 하고 다시 만들지 말 것.

**만든 것**

| 항목 | 내용 |
|---|---|
| **시작 가이드**(`src/components/onboarding-checklist.tsx`) | 홈 상단 4단계(단지 정보 → 세대 등록 → 직원 등록 → 모듈 구독). **완료 판정은 전부 실데이터** — "했다고 표시"를 저장하면 실제 상태와 어긋난 채 남는다. 전부 끝나면 스스로 사라지고 그전에도 닫을 수 있다. **소장에게만** 보인다(직원에게 자기가 못 하는 일을 할 일로 띄우지 않는다) |
| **체험 배너 강도** | D-3 이하·만료는 빨강, 그 위는 앰버. 30일 내내 같은 톤이면 아무도 안 읽는다. 만료·임박 배너에 카드 등록 링크를 붙였다 |
| **공지 링크**(`Announcement.linkUrl`/`linkLabel`) | 배너에 "자세히 보기" 버튼. **앱 안 경로(`/`로 시작)만 허용** — 외부 주소를 그대로 넣게 두면 공지 배너가 우리 이름으로 피싱 링크를 실어 나른다 |
| **작업 이력**(`/admin/logs`, P2 해소) | 운영자가 남의 단지에 한 일 7종 기록: 임퍼서네이션 진입·종료, 단지 상태 변경, 모듈 구독·체험 변경, 비밀번호 재설정, 직원 계정 발급. `src/lib/admin-log.ts`의 `logAdmin()` 한 줄로 붙인다 |
| **이탈 위험 목록**(수입 현황) | 7일 안에 체험이 끝나는데 **카드가 없는** 단지. 결제가 붙은 뒤로는 "체험 만료 예정"보다 이쪽이 실제 이탈 신호다 |

**AdminLog에 관계(FK)를 걸지 않은 것은 의도다.** 단지나 계정이 삭제돼도 "누가 무엇을 했는지"는 남아야 감사 로그다. 그래서 `adminName`·`tenantName`도 그 시점 값을 복사해 둔다. `logAdmin()`은 실패해도 원래 작업을 막지 않는다 — 로그 한 줄 때문에 단지 이용 중지가 안 되면 그게 더 큰 사고다(서버 로그에는 남긴다).

**만들지 않은 것 (다시 검토하지 말 것)**

| 뺀 항목 | 근거 |
|---|---|
| **운영자 배너 슬롯**(자동 슬라이드, 링크 타입 3종) | 우리 **공지 시스템이 이미 대상 7종 타게팅 + specificity 규칙**을 갖고 있다. 배너를 따로 만들면 타게팅 규칙을 두 벌 유지하게 된다. 실제로 부족했던 건 "링크 버튼" 하나뿐이라 **공지에 컬럼 2개**를 더해 끝냈다 |
| **일정 관리 + 위젯/Drawer** | 홈에 이미 할 일 위젯 4종이 있고, 법정 점검 주기는 **5단계 4번 모듈(법정 의무 이행 증빙)**이 담당하기로 확정돼 있다. 지금 범용 캘린더를 얹으면 그 모듈과 정면으로 겹친다 |
| **모듈 출시 알림 신청 창구** | 랜딩(`/`)과 한 몸인데 **사용자가 "랜딩은 나중에 개발"로 결정**했다. 랜딩 작업 때 같이 만들 것 |
| **DB·업로드 파일 자동 백업 크론** | `pg_dump`를 spawn하고 덤프를 둘 디스크가 필요하다 — **서버리스 배포에서는 실행 자체가 불가능**하고, 앱이 아니라 인프라 레이어(관리형 Postgres의 자동 백업 또는 서버 cron)가 할 일이다. 앱에 넣는 게 틀린 선택 |
| **로그인 실패 잠금 DB 이전** | 현재 프로세스 메모리(`src/lib/rate-limit.ts`)의 천장은 **이미 `ponytail:` 주석으로 표시돼 있다.** 아직 단일 인스턴스라 옮겨도 검증할 다중 인스턴스가 없다. 배포 아키텍처가 정해질 때 Redis든 DB든 한 번에 옮길 것 |

**검증**: smoke 30페이지(`/admin/logs` 추가) + 감사 로그 기록·라벨 매핑 확인(FK 없이 삭제된 단지 참조 포함)

### 5.8 (선택) FAQ + AI 자동응답

`building-manager`는 FAQ 68건 + Gemini 자동응답(신뢰도 낮으면 사람에게 이관) + 만족도 평가를 갖고 있다. 현 프로젝트는 문의만 있고 답변은 전화·이메일 수동 — 문의량을 줄이는 유일한 지렛대. 도입 시 Gemini가 아니라 Claude API로.

### 선행 프로젝트에서 **가져오지 않을 것** (판단 근거 포함)

- **사용자 수 제한 + 초과 과금**(`UserPricingSetting`/`maxUsers`) — 단지 정액 모델과 충돌. "체험·구독 단위는 단지 × 모듈, 직원 수 무관"이 이미 확정된 정책
- **3중 할인**(번들 + 이벤트 + 신규전환) — 판매 모듈이 0개인 지금 할인 엔진은 순수 부채
- **인보이스 PDF 첨부 메일** — 토스 영수증 URL로 충분. 선행 프로젝트도 "난이도 높음"으로 미뤘다
- 연차 등 **모듈 코드 전부** — 현 로드맵의 모듈 구성이 다르다

### 8. (후보) 월간 업무보고서 자동 생성
- 전 모듈 데이터 종합

### 9. (후보) 다단지 롤업 대시보드 — 위탁관리회사 본사용
- 멀티테넌트가 이미 있어 추가 비용이 낮고, **단지별 영업 대신 위탁사 단위 계약** 경로를 연다. 중소 위탁사는 자체 포털을 못 만든다

**보류 (착수하지 말 것)**
- 전자투표 — 이지스가 무료로 제공 중이라 유료화 불가
- ERP 코어(부과·수납·회계·검침)·세무 — 난이도 상 + 후발 진입 열위. **연동 파트너십이 답**

## 경쟁사 분석 (2026-07-27 조사)

### 시장 구조

국내는 네 층으로 갈라져 있고 각 층이 다른 지갑에서 돈을 받는다.
1. **ERP·회계층**(유료) — 이지스 XpERP(자사 주장 90%+ 점유), 더즌 더아파트, 야놀자 아파트테크. 부과·수납·회계·검침이 본체
2. **입주민 앱층**(관리사무소엔 무료, 광고·수수료로 수익) — 아파트너, 아파트아이, 모빌, 살다. **앱을 깔면 민원·전자결재·전자투표·공지가 공짜로 딸려온다** ← 디벅과 정면 충돌
3. **시설·장기수선 전문층** — 아파트너스/AFMS, 그리고 **아파트케어 '아지트'가 법령 RAG 챗봇 + 공고문·회의록 AI 자동작성 + 녹음 STT를 이미 무료 제공**
4. **공공** — K-apt(48개 항목 의무 공시, 전자입찰)

**핵심 함의: 뚫어야 할 벽은 기능이 아니라 "이미 무료로 깔려 있다"이다.**

### 로드맵 조정 근거 (위 5단계 목록에 반영 완료)

| 반영된 항목 | 근거 |
|---|---|
| 2번에 K-apt 입찰 리드타임 역산 추가 + 순위 상승 | 공고는 마감 전일 기산 10일 전 의무라 만료일 알림만으론 늦다. 조사 범위 내 경쟁사 중 하는 곳 없음 — 저난이도 고체감 |
| 1번을 에스컬레이션 파이프라인으로 재설계 | 1차·2차·내용증명·지급명령까지 법적 단계가 정해져 있음. PayProp·Yardi·Enumerate 공통 형태이고 이지스도 독촉을 ERP에 이미 보유 — 단발 문서면 ERP 부가기능과 구별 불가 |
| 3번을 워크플로 + SLA로 승격 | 해외 6개 제품 전원이 이 형태. 국내 무료 앱층은 접수·배정까지만 제공 — 무료 경쟁사가 안 하는 지점이 정확히 여기 |
| AI 모듈(6·7번) 순위 하락 + 파이프라인으로 재정의, 전자결재 5번으로 상승 | 아지트가 이미 무료로 공고문·회의록 생성과 녹음 STT 제공 중(월 12,000건). 생성만으로는 유료화 근거 없음 — 못 하는 건 결재·채번·보관으로 흘려보내는 것 |
| 5번+8번을 4번 "법정 의무 이행 증빙"으로 병합 | 관리사무소 전용 산업안전보건·중대재해처벌법 대응 솔루션은 조사에서 확인되지 않음 — 화이트스페이스 가능성 |
| 9번 다단지 롤업 신규 추가 | 구매 결정권자가 위탁사 본사인 경우가 많음. 우리관리는 자체 포털을 만들었지만 중소 위탁사는 못 만든다 |

### 차별화 축 (이미 가진 것)

- **ERP를 대체하지 않고 얹히는 포지션** — 관리사무소는 이지스를 못 버린다(수납 VAN·K-apt 연동). "ERP가 안 해주는 행정·문서 업무만 추가 구독"이면 교체 저항이 0
- **문서번호 자동채번 + 문서함 + 결재선 조합** — "지자체 감사 나와도 3분 만에 뽑는 서류철"로 패키징하면 구매 동기가 명확해짐
- **모듈 단위 구독 = 소규모 단지 진입 가능** — 300세대 미만 7,260곳 중 44.8%가 직원 1~3명, 소장 56.7%가 인력 부족 응답. AppFolio(50유닛 미만 거절)·Yardi Breeze(최소 $400/월)식 최소요금 장벽이 없음
- **국내 전원 가격 비공개** — 공개 요금표 + 무료 체험을 국내에서 처음 하면 그 자체가 마케팅 자산 (체험 기능은 이번에 구현됨)
- **판매 채널**: 중소 위탁관리회사 본사. 멀티테넌트가 이미 있으니 **다단지 롤업 대시보드 하나만 얹으면** 단지별 영업 대신 위탁사 단위 계약 가능

*확인 안 된 것: 국내 전 제품의 구체 가격·요금제(전원 비공개), 국내 경쟁사의 문서번호 채번 보유 여부, K-apt 외부 연동 API 제공 범위. 점유율 수치는 모두 자사 발표.*

## 선행 프로젝트 `building-manager` (참조용, 2026-07-27 분석)

**같은 서비스의 최초 버전.** 이것을 만들다가 점검·개선 내용을 반영해 현 프로젝트(`dibuck_saas`)를 새로 만들고 있다. 위치: `D:\01. claude code\building-manager` (Express + React/Vite + Prisma, 현 프로젝트와 스택이 다르다 — **코드를 복사하는 게 아니라 설계·플로우를 옮긴다**).

**핵심 가치: SaaS 플랫폼 층이 현 프로젝트보다 훨씬 완성돼 있다.** 특히 결제 운영(PAST_DUE 유예·재시도·셀프 재결제·일할 계산)과 메일 템플릿 12종은 5.6·5.5의 설계도 역할을 한다. 5.5·5.6·5.7 절에 무엇을 가져오고 무엇을 버릴지 정리해 뒀다.

읽을 파일:
- `saas-master-plan.md` — 완료/미완료 전체 목록과 판단 근거
- `backend/src/utils/mailer.js` — 메일 템플릿 12종 (5.5)
- `backend/src/utils/scheduler.js` — 크론 8종(체험 만료·갱신 알림·정기결제·PAST_DUE 재시도·백업) (5.6)
- `backend/src/utils/feeCalculator.js` — 일할 계산·요금 분해 (5.6)
- `frontend/src/components/OnboardingChecklist.jsx`, `SubscriptionBanner.jsx`, `Dashboard.jsx` (5.7)

⚠️ 모듈 코드(연차 등)는 보지 말 것 — 현 로드맵의 모듈 구성이 완전히 다르다.

## 개발 환경

- Next.js 16(App Router) + TypeScript + Tailwind v4 + shadcn/ui, PostgreSQL 18 + Prisma 7(`prisma-client` 생성기, `@prisma/adapter-pg`, DB URL은 `prisma.config.ts`)
- 실행: `npm run dev` → http://localhost:3000
- 데모 계정 (비밀번호 모두 `test1234`): `test1@test.com`(소장) / `test2@test.com`(경리) / `test3@test.com`(직원) / `admin@test.com`(관리자)
- 운영 최초 관리자: `.env`에 `ADMIN_EMAIL`·`ADMIN_PASSWORD`(12자 이상, 선택 `ADMIN_NAME`)를 넣고 시드 실행. 데모 데이터는 `NODE_ENV=production`이면 건너뛴다
- 스모크 테스트: `npx tsx smoke.ts` (dev 서버 실행 중이어야 함, 26페이지 검사)
- 카카오 로그인은 `.env`에 `KAKAO_REST_API_KEY`(+선택 `KAKAO_CLIENT_SECRET`)를 넣어야 활성화 — 없으면 로그인 페이지에서 버튼 숨김
- 메일 발송은 `.env`에 `SMTP_HOST`·`SMTP_USER`·`SMTP_PASS`(+선택 `SMTP_PORT`·`SMTP_SECURE`·`SMTP_FROM`)를 넣어야 활성화 — 없으면 비밀번호 셀프 재설정 UI가 숨고 수동 재설정 안내로 대체된다. **배포 시 `APP_URL`을 실제 도메인으로** (메일 링크가 이 값을 쓴다)
- 결제는 `.env`에 `TOSS_SECRET_KEY`·`NEXT_PUBLIC_TOSS_CLIENT_KEY`를 넣어야 활성화 — 없으면 설정 > 결제가 "결제 준비 중" 안내로 대체된다. 심사 전에도 **토스 테스트 키로 전 흐름을 확인할 수 있다**
- 크론 인증용 `CRON_SECRET` 필수 — 없으면 `/api/cron/billing`·`/api/cron/training`이 401로 막힌다(fail closed). **배포 시 새 값으로 바꿀 것**
- AI 초안 생성은 `.env`에 `ANTHROPIC_API_KEY` — 없으면 기안·품의 모듈의 **생성만** "준비 중"으로 막히고 폼·판정·문서 열람은 그대로 돈다. 개발 키는 발급 완료(2026-07-27). 키는 조직 단위 자격증명이라 **모듈마다 나누지 않는다** — 나누는 기준은 프로젝트(placelink ↔ 디벅)와 환경(개발 ↔ 운영). 콘솔 워크스페이스별 지출 한도를 걸어 두는 게 만료일보다 실효가 크다
- 순수 로직 검증: `npx tsx metrics.test.ts` (MRR·체험 제외) / `npx tsx announcements.test.ts` (공지 대상 판정) / `npx tsx dates.test.ts` (KST 날짜·이메일 정규화) / `npx tsx rate-limit.test.ts` (시도 횟수 제한) / `npx tsx password-reset.test.ts` (재설정 토큰 상태) / `npx tsx billing.test.ts` (청구 계산·크론 판정) / `npx tsx gian-rules.test.ts` (금액·문서분류·결재선·한글금액) / `npx tsx approval-flow.test.ts` (외부 서명 토큰 fail-closed) / `npx tsx safety-training.test.ts` (교육시간 파싱·인원별 누적·안내 회차)
- DB 필요: `npx tsx modules.test.ts` (모듈 노출·잠금 규칙) / `npx tsx billing-run.test.ts` (청구 상태 전이) / `npx tsx approval-run.test.ts` (상신→반려→재상신→순차 승인→외부 토큰→완료)
- **실 API 호출(비용 발생, 자동 검증 아님)**: `npx tsx gian-draft.manual.ts [0|1|2]` — 프롬프트를 손볼 때 결과를 눈으로 보는 스크립트(0=품의서 수의계약, 1=기안서 예산없음, 2=공사추진 장충금+입찰). 건당 ~15원
- ⚠️ 스키마 변경 후 `prisma generate` 하면 dev 서버 재시작 필요 (구버전 클라이언트 캐시로 500 발생)
